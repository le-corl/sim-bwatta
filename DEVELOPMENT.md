# 개발 기록

`심봤다`는 2026 Apps in Toss 바이브코딩 챌린지를 계기로 시작한 짧은 미니게임입니다. 이 문서는 세션별 작업 일지나 출시 행정 기록이 아니라, 완성된 마일스톤에서 확인된 제품 결정·구현 결과·검증 교훈만 공개용으로 증류합니다.

## 제품 방향

핵심 경험은 두루마리 휴지를 빠르게 풀다가 작은 행동 미션을 만나고, 휴지를 모두 낭비한 보상으로 황당한 휴지심을 얻는 것입니다. 기능을 많이 넣기보다 한 손으로 즉시 이해할 수 있는 조작, 짧은 성취감, 예상 밖의 타이밍에서 오는 웃음을 우선했습니다.

- 화면은 `휴지심 보관함`과 `플레이` 두 개만 사용합니다.
- 아래 방향 스와이프만 진행에 반영하고 위 방향 입력은 무시합니다.
- 대각선 이동과 손의 좌우 흔들림은 보정하되 작은 떨림은 제거합니다.
- 스와이프 거리와 속도를 분리해 기본 감도와 가속감을 각각 조정합니다.
- 행동 미션은 90초 가이드만 제공하며 조기 완료와 시간 초과를 모두 허용합니다.
- 진행 중 회차는 영구 저장하지 않고, 휴지를 모두 푼 완료 순간에만 결과를 저장합니다.
- 결과는 일반 또는 황금 휴지심으로 남고 PNG 저장과 공유 진입점을 제공합니다.

## UX가 완성된 과정

초기 구현은 휴지 롤의 형태보다 진행률과 상태 전환을 먼저 검증했습니다. 이후 실제 사용자가 보는 핵심 가치가 “휴지를 막 내리는 느낌”이라는 판단에 따라 시각 구조를 여러 차례 다듬었습니다.

### 입력과 진행

비가역 조작은 각 포인터 이동에서 아래 방향 성분만 누적하는 방식으로 구현했습니다. 기준점을 매 이동마다 갱신해 위로 흔들렸다가 다시 아래로 당기는 부정확한 동작도 자연스럽게 이어집니다. 미션 체크포인트를 넘긴 입력은 잘라내고 정확한 위치에서 멈추므로, 상태와 화면이 서로 앞서지 않습니다.

기본 이동 감도인 `swipeWeight`와 속도 이득인 `swipeAcceleration`은 별도 설정입니다. 이를 통해 완료까지 필요한 스와이프 횟수와 한 번 세게 당겼을 때의 쾌감을 독립적으로 조정할 수 있습니다.

### 휴지 롤과 속도감

휴지 롤은 전면의 종이가 몸통을 덮고 오른쪽 단면과 축이 보이는 벽걸이 구도로 정리했습니다. 새 롤과 휴지심의 외경은 실제 약 `11:4` 비율을 반영하며, 휴지가 풀릴수록 폭은 유지하고 외경만 줄어듭니다. 내려오는 휴지는 오른쪽 단면의 왼쪽 접점에서 수직으로 이어지고, 바깥쪽 종이를 당기는 방향에 맞춰 단면은 반시계방향으로 회전합니다.

회전, 절취선 이동, 휴지 길이, 돌발 칸은 모두 실제 반영된 진행률 하나에서 파생합니다. 빠른 입력은 전환 시간과 짧은 잔상으로 강조하지만 목표 위치는 바꾸지 않습니다. 특히 휴지와 조작 안내를 별도 레이어로 나눠, 휴지 밑단은 실제 이동량만큼 화면 아래로 내려가고 `↓ 아래로` 안내는 읽을 수 있는 범위에 남도록 했습니다.

100%에 도달했을 때 결과 창을 즉시 띄우면 완료감이 약했습니다. 그래서 입력을 잠근 뒤 마지막 휴지가 심에서 분리되어 아래로 떨어지고, 드러난 휴지심이 짧게 강조된 다음 결과를 저장하고 팝업을 표시하도록 순서를 바꿨습니다.

### 예상 밖의 한 칸

반복 무늬만으로는 랜덤한 재미가 부족해 짧은 글자나 낙서가 지나가는 돌발 휴지 칸을 추가했습니다. 첫 완료 기록이 없는 플레이에는 한 번 보장하되, 이후에는 직전 완료 판에 등장했다면 한 판 쉬고 그 외 판에서 확률적으로 등장합니다. 미션 구간과 완료 직전은 피하고, 플레이를 멈추거나 팝업을 띄우지 않습니다.

돌발 칸과 절취선을 서로 다른 위치 애니메이션으로 움직였을 때 속도 차이가 눈에 띄었습니다. 두 요소를 같은 이동 컨테이너에 넣고 단일 `transform`을 공유해 빠르게 지나가더라도 휴지 표면에 붙어 있는 것처럼 보이게 했습니다.

## 저장과 재방문

보관함은 완료 결과만 최신순으로 저장합니다. 앱 종료나 WebView 재생성으로 진행 중 판이 사라지는 것은 의도한 정책이며, 완료 결과는 팝업 표시 전에 저장합니다. 서버·로그인·클라우드 동기화는 현재 범위에 포함하지 않습니다.

짧은 게임을 한 자리에서 과소비하는 문제에는 일일 횟수 제한 대신 단계형 리필을 적용했습니다. 첫 두 판은 연속으로 허용하고, 이후 완료 시점부터 `5분 → 30분 → 2시간 → 5시간` 간격을 둡니다. 단계는 현지 시각 오전 6시에 초기화되고 중도 종료는 소비로 계산하지 않습니다. 쿨다운 중에도 보관함과 저장·공유는 사용할 수 있습니다.

리필은 장기 보상의 대체물이 아니라 콘텐츠 소비 속도를 조절하는 장치입니다. 이후 재방문 개선은 제한 강화보다 새로운 문구·돌발 칸·휴지심 형태와 수집 목표를 확장하는 순서가 적합하다고 판단했습니다.

## 기술 구조

- React 19, TypeScript, Vite 기반 WebView 앱
- Apps in Toss WebView SDK 3.1.1
- 서버 없는 로컬 우선 구조
- Canvas 기반 결과 카드 PNG 생성
- Toss와 브라우저 PWA를 분리하는 Vite mode 구성

Toss 빌드에서는 공식 `Storage`, `File.saveBase64`, `Share.sendMessage`, Analytics와 익명 사용자 키를 사용합니다. 웹 PWA에서는 같은 게임 코드를 유지하면서 `localStorage`, 브라우저 다운로드, Web Share 또는 클립보드로 대체합니다. 플랫폼 분기는 모듈 진입부에서 일찍 결정해 웹 번들에 Toss 전용 SDK와 딥링크가 섞이지 않게 했습니다.

GitHub Pages 프로젝트 경로에서도 동작하도록 Vite `base`, PWA manifest의 `start_url`·`scope`, service worker의 캐시 기준을 같은 하위 경로에 맞췄습니다. Toss `.ait`에는 PWA 전용 파일이, PWA 산출물에는 Toss SDK가 들어가지 않는지 각각 검사합니다.

## 마일스톤

| 마일스톤 | 완료 결과 |
|---|---|
| M0 — 개발 환경 | 저장소 규칙과 로컬 도구 체계를 구성하고 반복 가능한 빌드 환경을 확인했습니다. |
| M1 — 콘셉트와 골격 | 핵심 루프·범위·기술 스택을 확정하고 로컬 실행과 첫 `.ait` 빌드를 완료했습니다. |
| M2 — 핵심 루프 | 스와이프, 미션, 완료 연출, 로컬 보관함, 이미지 저장·공유를 실기기에서 검증했습니다. |
| M3 — UX QA | 실제 휴지 구도와 속도감, 돌발 칸, 보관함 UI를 반복 조정하고 회귀 검증했습니다. |
| M4 — 챌린지 제출 | `0.2.1` 번들과 별도 출품 신청을 완료했습니다. |
| M5 — 재방문 루프 | 단계형 리필과 최소 이벤트 계측을 구현하고 자동·PC 검증을 마쳤습니다. |

## 검증 원칙과 배운 점

- 상태와 애니메이션은 동일한 진행률을 기준으로 계산해야 체크포인트와 빠른 입력에서 어긋나지 않습니다.
- 움직이는 사물과 읽어야 하는 안내는 레이어를 분리해야 속도감과 가독성을 함께 유지할 수 있습니다.
- 저장 시점을 완료 한 곳으로 제한하면 중도 종료 정책과 데이터 일관성이 단순해집니다.
- 브리지 API 실패, Analytics 실패, 공유 기능 부재는 핵심 플레이와 분리해야 합니다.
- 자동 테스트와 빌드만으로 시각 문제를 찾을 수 없습니다. 모바일 크기 렌더와 실제 기기에서 전체 루프를 반복 확인했습니다.
- 공개용 스크린샷과 결과 이미지는 최종 크기로 렌더링해 잘림·겹침·문구 배치를 직접 확인합니다.

## AI 협업 방식

사용자는 제품 방향과 체감 품질을 결정하고 직접 시각 QA를 수행했습니다. Codex는 결정 문서화, 구현, 자동 검사와 작업 기록을 맡았습니다. 결정과 구현을 구분하고 한 번에 하나의 작업만 진행했으며, 각 배치가 끝날 때 테스트와 빌드 결과를 확인했습니다.

## 문서 관리 원칙

`DEVELOPMENT.md`는 마일스톤이 완료된 시점에만 갱신합니다. 각 갱신에는 목표, 핵심 결정, 구현 결과, 검증 방법, 재사용 가능한 교훈만 남깁니다.

계정 정보, 콘솔 식별자, 신원 인증, 출시 행정 절차, 로컬 장비 경로, 세션별 상세 로그는 이 문서와 공개 저장소에 포함하지 않습니다. 진행 중인 작업이나 아직 검증되지 않은 계획도 완료 이력처럼 기록하지 않습니다.
