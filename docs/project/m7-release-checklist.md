# M7 사용자 체크리스트 — 행정 증빙·검수·공개

updated: 2026-08-28

## 현재 공개 후보

- 파일: `sim-bwatta.ait`
- 앱 버전: `0.3.1`
- appName: `sim-bwatta`
- SDK: `3.1.1`
- deploymentId: `01a04311-f7e7-7265-ac28-9a6fd8d153d7`
- SHA-256: `A107F3750702B32B0BF6AE59F82A2874EC44ECBE7291D47C44B22ED86A13DED7`
- 상태: 자동 검증 완료, 최종 실기기 회귀 진행 중. 실제 시간 대기가 필요한 리필 항목만 남았고 X 종료 확인은 M7-4로 이관함

## 1. 0.3.1 업로드와 최종 실기기 회귀

- [x] 콘솔 버전 관리에서 위 `sim-bwatta.ait`를 새 버전으로 등록한다.
- [ ] 메모는 `0.3.1 공개 후보 / 리필 CTA·사용자 식별키·브랜드 색상`으로 남긴다.
- [x] 업로드된 항목의 SDK 버전이 `3.1.1`인지 확인한다.
- [x] `테스트`로 QR 또는 푸시를 열어 한 번 이상 실행한다. 검토 요청 버튼 활성화에 필요한 절차다.
- [x] 미니앱 내부의 게임용 상단 내비게이션과 닫기(X) 버튼이 노출되고 Safe Area와 겹치지 않는지 확인한다.
- [→ M7-4] X 버튼 종료 확인 창은 공개 라이브에서 다시 확인한다. 비공개 테스트에서 확인 창 없이 토스 네이티브 화면으로 즉시 전환되지만, 비공개 환경에 한정된 동작일 가능성을 공식 문서만으로 배제할 수 없어 현재 M6·M7 블로커로 취급하지 않는다.
- [x] 앱 첫 화면이 10초 안에 열리고 세로 화면·Safe Area·확대 방지·뒤로가기 제스처 차단이 정상인지 확인한다.
- [x] 한 회차를 `스와이프 → 미션 → 복귀 → 100% 분리 연출 → 결과 저장`까지 완료한다.
- [x] 앱 종료·재실행 후 완료된 휴지심은 유지되고 진행 중이던 회차만 초기화되는지 확인한다.
- [ ] 상세 화면의 이미지 저장과 결과 공유가 네이티브 기능으로 동작하는지 확인한다.
- [x] 같은 제품 일자에 두 회차를 완료한 뒤 청회색 리필 CTA, 절대 해제 시각, 실시간 타이머, 플레이 차단을 확인한다.
- [x] 5분 뒤 다시 진입해 CTA가 활성화되고 다음 회차가 시작되는지 확인한다.

X 버튼의 라이브 확인을 제외한 이 항목을 모두 통과하면 M6-3을 종료하고 아래 M7 검수 요청으로 넘어간다.

`게임 내비게이션`은 토스의 공개 게임 검색 목록이 아니라 미니앱 안에 자동 표시되는 상단 바를 뜻한다. `심봤다`가 토스 게임 검색에 나오지 않는 것은 아직 검토·출시 전이므로 이 실기기 회귀 항목과 별개이며, 공개 검색 노출은 M7-4에서 확인한다.

## 챌린지 출품 자격 확인

- 2026-08-26까지 필요한 조건은 별도 출품 신청폼 제출과 앱 번들 등록이다.
- 공식 안내는 앱 정보가 승인되지 않아도 버전 등록이 가능하며, 등록 후 순차 검수를 기다리라고 명시한다.
- 따라서 8월 26일까지 검토 완료·출시까지 요구된 것은 아니다. 현재 검토 전 상태 때문에 챌린지 출품이 자동 무효가 되지는 않는다.
- 다만 9월 첫째 주 테마 지면 노출과 9월 1~27일 지표 확보를 위해 실제 공개는 계속 최우선이다.

## 2. 등급분류 경로 확정 — 현재 최우선 블로커

아래 경로 중 하나만 최종 선택한다.

### A. Google Play 동일 게임 공개 URL

- [x] Google Play 경로를 등급분류 경로로 선택한다.
- [x] 앱 생성 입력값을 확정한다: `심봤다` / `com.double_b.sim_bwatta` / 한국어(`ko-KR`) / 게임 / 무료.
- [x] 사용할 Google Play 개발자 계정이 개인 계정임을 확인한다.
- [ ] 위 값으로 Google Play Console 앱 생성을 완료한다.
- [ ] 계정 생성일 또는 앱 대시보드의 `프로덕션 액세스 신청` 안내로 신규 개인 계정 테스트 요건 적용 여부를 확인한다.
- [ ] 요건 대상이면 앱 설정을 완료하고 비공개 테스트 트랙에 출시한다.
- [ ] 최소 12명의 테스터가 참여를 선택한 상태를 14일 연속 유지한다. 중도 참여 해제자는 다시 참여한 날부터 14일을 새로 계산한다.
- [ ] 테스터가 핵심 기능을 실제 사용하게 하고 버그·사용성 의견과 반영 내용을 기록한다. 프로덕션 액세스 신청 때 참여 방식과 피드백 요약이 필요하다.
- [ ] Play Console 집계가 충족되면 프로덕션 액세스를 신청한다. 검토는 일반적으로 7일 이내지만 더 오래 걸리거나 추가 테스트가 필요할 수 있다.
- [ ] 프로덕션 액세스 승인 후 프로덕션 트랙에 제출한다.
- [ ] 스토어에 공개할 게임의 이름·핵심 플레이·콘텐츠·등급 정보가 앱인토스판과 동일하도록 구성한다.
- [ ] 실제 공개된 Google Play URL을 확보한다. 비공개·테스트 트랙 링크는 사용하지 않는다.
- [ ] 게임물관리위원회 `자체등급분류 게임물 조회`에서 아래 값을 그대로 옮긴다.
  - 등록자명(구글 플레이 개발자명)
  - 자체등급분류사업자명(구글)
  - 등급분류일자
  - 등급분류번호
  - 이용등급
  - 내용정보 전체
- [ ] 흰 종이에 한 대표자 사인 이미지를 준비한다.
- [ ] Google Play판 원본 플레이 화면 2장과 앱인토스판 원본 플레이 화면 2장을 준비한다. 자르기·문구 추가·재구성을 하지 않는다.

### B. 게임물관리위원회 직접 심의

- [ ] 직접 심의를 신청하고 발급된 등급분류증명서 PDF를 준비한다.
- [ ] 증명서의 등록자명·등급분류일자·번호·이용등급·내용정보를 콘솔에 그대로 입력한다.
- [ ] 콘솔이 요구하는 원본 앱인토스 플레이 화면을 준비한다.

기존 선택은 A였으나 실제 12명·14일 블로커가 확인되어 후순위로 내렸다. 2026-08-27에 12명이 모두 참여해도 프로덕션 액세스 신청은 빠르면 2026-09-10 전후다. 다른 게임의 URL이나 등급번호는 재사용하지 않는다.

### C. Microsoft Store 동일 게임 PWA — 최종 선택

- [x] Microsoft 개인 개발자 등록과 계정 검증을 완료한다.
- [x] Partner Center의 `Apps & Games`에 진입한다.
- [x] `새 제품 → MSIX 또는 PWA 게임`에서 고객에게 표시할 Store 제품명 `심봤다`를 예약한다. 제품 개요에 `심봤다 / 게임 / 초안`과 `제품 ID 보기`가 표시되어 예약·게임 제품 생성을 확인했다.
- [x] 동일 핵심 게임의 `web-pwa` 배포 대상을 만들고 로컬 빌드·화면·Toss 회귀를 검증한다.
- [x] GitHub Pages workflow를 실행해 `web-pwa`를 HTTPS에 공개하고 URL을 확인한다. 공개 URL은 `https://le-corl.github.io/sim-bwatta/`이며 Actions 재실행의 build·deploy가 모두 성공했다.
- [x] PWABuilder에서 Windows Store 패키지 `1.0.1`을 생성하고 `.msixbundle`의 제품명·Package ID·Publisher ID·Publisher 표시명·한국어 리소스·공개 PWA URL을 검증한다.
- [x] Apps in Toss 플랫폼과 Microsoft Store/Web PWA 플랫폼의 처리 차이를 구분한 개인정보 처리방침을 `https://le-corl.github.io/sim-bwatta/privacy.html`에 공개한다.
- [x] Partner Center에 `.msixbundle`을 업로드하고 IARC 설문, 스토어 설명·이미지, 속성, 가격 및 가용성을 입력해 인증 제출한다. 현재 상태는 `인증 중`, 단계는 `사전 처리 중`이다.

PWABuilder 재검사에서 manifest와 service worker의 패키징 필수 조건을 모두 통과했다. 웹 화면의 패키지 버튼이 반응하지 않아 동일한 Microsoft 공식 PWABuilder 패키징 API로 생성했으며, Store 업로드 대상은 Git 제외 로컬 경로의 `심봤다_1.0.1.0.msixbundle`이다. 앱 SemVer `0.3.1`과 Windows Store 패키지 버전 `1.0.1.0`은 각 플랫폼 규칙에 따라 별도로 관리한다.

- [ ] 최대 3영업일 인증 결과를 확인하고 공개 URL을 확보한다.
- [ ] 게임물관리위원회 자체등급분류 조회 반영을 확인한다.
- [ ] Microsoft Store 공개 URL과 조회 정보를 Apps in Toss 콘솔에 입력한다.

Google 경로는 실제 12명·14일 블로커가 확인되어 후순위로 내렸다. Microsoft Store PWA를 주 경로로 확정했으며 세부 비교는 [`store-route-options.md`](./store-route-options.md)를 따른다.

## 3. 콘솔 앱 정보 완성

- [ ] 앱 이름 `심봤다`, appName `sim-bwatta`, 유형 `게임`, 카테고리 `인디`, 웹보드 게임 `아님`을 재확인한다.
- [ ] 개발자 정보에는 현재 유효한 개인 개발자 정보를 입력한다.
- [ ] 고객문의 이메일과 연락처가 실제 응답 가능한 값인지 확인한다.
- [ ] 앱 로고 600×600, 썸네일 1932×828, 노출용 스크린샷을 최종 확인한다.
- [ ] 앱 검색 키워드와 게임 설명이 실제 기능만 설명하고 있는지 확인한다.
- [ ] 2번에서 확보한 등급 정보·URL 또는 증명서·원본 플레이 화면을 등록한다.
- [ ] 개인정보처리방침 입력란에 `https://le-corl.github.io/sim-bwatta/privacy.html`을 등록한다. 문서와 실제 Apps in Toss 플랫폼의 로컬 기록·익명 식별키·Analytics 이벤트가 일치하는지 최종 확인한다.
- [ ] 주요 기능 진입점은 루트 스킴 `intoss://sim-bwatta`로 등록하고 설명은 `두루마리 휴지 풀기 및 휴지심 보관`으로 맞춘다.
- [ ] 앱 정보 검토를 요청한다. 공식 예상 시간은 영업일 1~2일이다.

## 4. 번들 검토 요청

- [ ] 1번 실기기 회귀를 통과한 `0.3.1` 버전만 선택한다.
- [ ] `검토 요청`을 누르고 운영·기능·디자인·보안 검토 상태를 확인한다.
- [ ] 검토 중에는 새 번들을 올리지 않는다. 문제가 발견되면 검토를 취소하고 PATCH 버전으로 수정·재검증한 뒤 다시 요청한다.
- [ ] 반려 사유가 오면 문구를 그대로 기록하고, 범위를 늘리지 않은 최소 수정만 적용한다.

번들 검토는 공식 안내상 영업일 기준 최대 3일이므로 등급 증빙과 앱 정보 등록을 먼저 병행한다.

## 5. 승인 후 공개와 라이브 확인

- [ ] 콘솔에서 `출시하기`를 누른다.
- [ ] 라이브 주소 `https://sim-bwatta.apps.tossmini.com`으로 실제 토스 앱에서 진입한다.
- [ ] 라이브에서 첫 화면 진입, 한 회차 완료, 이미지 저장·공유, 앱 재실행 후 기록 유지를 확인한다.
- [ ] 라이브에서 X 버튼을 눌렀을 때 게임 종료 확인 창의 취소·종료가 모두 동작하는지 확인한다. 여전히 즉시 종료되면 앱인토스 채널톡에 문의한다.
- [ ] 치명 오류가 없으면 해당 버전을 유지하고 기능 추가를 중단한다.
- [ ] 공개 다음 날 Analytics에서 `roll_start`, `roll_complete`, `refill_locked`, `refill_return` 수집 여부를 확인한다.
- [ ] 챌린지 지표 기간인 2026-09-01~2026-09-27 동안 M8 일별 기록을 시작한다.

## 오늘 바로 할 일

1. [완료] `새 제품 → MSIX 또는 PWA 게임`에서 `심봤다` 이름 예약
2. [완료] `제품 ID 보기`의 패키지·Publisher·Store 식별자를 Git 제외 로컬 파일에 기록
3. [완료] 동일 게임 `web-pwa` 제작·로컬 검증
4. [완료] GitHub Pages HTTPS 공개와 URL 확인
5. [완료] PWABuilder Windows Store 패키지 생성·식별자 검증
6. [완료] 공통 개인정보 처리방침 작성·GitHub Pages 공개
7. [완료] Partner Center 패키지 업로드·IARC·스토어 정보 입력·인증 제출
8. Microsoft 인증·게시 완료와 공개 URL 확인
9. 앱인토스 채널톡에 Microsoft Store 공개 후 게임물관리위원회 조회 반영 전 처리 방법 문의
10. 등급 증빙 확보 뒤 `0.3.1` 업로드·최종 실기기 회귀 및 앱인토스 검토 요청
