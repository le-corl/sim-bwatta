# 작업 및 학습 기록

updated: 2026-08-28

형식: `날짜 / 작업 ID / 결정·구현·교훈·실패`.

## 로그

- 2026-08-25 / M0-1 / 결정: 개발과 성장성 캐시는 E: USB 3 SSD에 둔다. D: HDD는 보관용으로 남긴다.
- 2026-08-25 / M0-2 / 구현: Claude 전용 자동 로딩 지점을 Codex의 `AGENTS.md`와 `.agents/skills` 구조로 변환했다.
- 2026-08-25 / M0-3 / 결정: 전체 `CODEX_HOME` 이전은 인증·설정·플러그인 영향 때문에 마감 후 검토한다.
- 2026-08-25 / M0-3 / 구현: 프로젝트 Node, npm/pnpm 캐시, Codex SQLite 상태 경로를 E: 프로젝트의 `.local/`로 지정했다.
- 2026-08-25 / M0-4 / 구현: Node v24.19.0, npm v11.17.0, npm·pnpm E: 캐시, Node 배포본 SHA-256, Codex TOML, `toss-lead`·`toss-pair` 스킬 검증을 통과했다.
- 2026-08-25 / M1-1 / 결정: 하루 최대 3회, 휴지 걸이를 돌려 휴지를 모두 낭비하면 휴지심 결과가 나오는 로컬 미니게임으로 정했다.
- 2026-08-25 / M1-1 / 결정: 감정 목표는 귀여움·킹받음·랜덤 당황이며, 결과는 로컬 다이어리와 이미지 공유로 연결한다.
- 2026-08-25 / M1-1 / 결정: 앱인토스 준비는 처음부터 시작하며 제출을 학습보다 우선한다.
- 2026-08-25 / M1-3 / 결정: WebView + React + TypeScript + SDK 2.x, Apps in Toss `Storage`, 서버 없는 구조를 사용한다.
- 2026-08-25 / M1-3 / 확인: 공식 API는 Base64 파일 저장과 메시지 공유를 제공한다. 직접 이미지 첨부 공유는 확정하지 않고 PNG 저장을 폴백으로 둔다.
- 2026-08-25 / M1-2 / 결정: 행동 미션형을 선택했다. 휴지 돌리기 → 중간 행동 미션 1~2회 → 황당한 랜덤 휴지심 보상의 3구간으로 구성한다.
- 2026-08-25 / M1-2 / 결정: 중간 미션을 완료하면 휴지 돌리기로 복귀하고, 최종 휴지심은 로컬 다이어리에 보관한다.
- 2026-08-25 / M1-2 / 확인 필요: 미션당 약 2분에 최대 2회면 기존 한 판 3분 제한을 초과하므로 시간 규칙을 재결정해야 한다.
- 2026-08-25 / M1-2 / 결정: 행동 미션은 1회 보장, 두 번째는 30% 확률로 등장시켜 기대 횟수를 1.3회로 맞춘다.
- 2026-08-25 / M1-2 / 결정: 미션 1회 판은 일반 휴지심, 2회 판은 황금 휴지심과 특별 연출로 보상한다.
- 2026-08-25 / M1-2 / 결정: 목표 기대 런타임은 2분 40초이며, 개별 판의 최대 시간이 아니라 평균 목표로 해석한다.
- 2026-08-25 / M1-2 / 확인 필요: 미션당 2분이면 기대 미션 시간만 2분 36초이므로 휴지 돌리기를 포함한 2분 40초 목표와 시간 예산이 맞지 않는다.
- 2026-08-25 / M1-2 / 결정: 미션당 목표 평균 90초, 휴지 돌리기·전환 판당 합계 43초로 정해 기대 런타임을 약 160초로 맞춘다.
- 2026-08-25 / M1-2 / 결정: 미션 1회 판은 약 2분 13초, 황금 휴지심이 나오는 2회 판은 약 3분 43초를 허용한다.
- 2026-08-25 / M1-2 / 결정: 앱 표시 이름은 `심봤다!`, 콘솔용 `appName`은 `sim-bwatta`로 확정했다.
- 2026-08-25 / M1-4 / 작업 발행: 현재 루트에 WebView + React + TypeScript 앱을 구성하고 의존성 설치, 로컬 실행, 프로덕션 빌드를 검증한다.
- 2026-08-25 / M1-4 / 구현: 현재 루트에 React 19.2.8, Apps in Toss WebView 3.1.1, Vite 7.3.6, TypeScript 5.9.3 프로젝트를 구성했다.
- 2026-08-25 / M1-4 / 실패: Vite 8 + React 플러그인 6 설치가 Apps in Toss CLI의 Babel 7 의존성과 충돌했다. 강제 설치하지 않고 Vite 7 + 플러그인 5로 낮춰 해결했다.
- 2026-08-25 / M1-4 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. Vite 빌드와 `ait build`가 완료되어 66,822바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M1-4 / 검증: `. .\scripts\activate-dev.ps1; npm run dev -- --host 127.0.0.1` 실행 후 `Invoke-WebRequest http://127.0.0.1:5173/`에서 HTTP 200, 제목 `심봤다!`, 루트 엘리먼트를 확인했다.
- 2026-08-25 / M1-4 / 검증: `npm ls --depth=0` 성공, 설치 트리의 누락·중복 오류 없음. 설치 감사 결과 취약점 0건.
- 2026-08-25 / M1 리뷰 / 완료: 콘셉트·범위·이름·기술 스택과 WebView 스캐폴드의 로컬 실행·`.ait` 빌드를 확인해 M1을 완료 처리했다.
- 2026-08-25 / M1 리뷰 / 정정: 실제 최신 안정판과 CLI 산출물에 맞춰 `SDK 2.x만 사용`을 `WebView SDK 3.1.1`, `apps-in-toss.config.ts`, `ait build`로 수정했다.
- 2026-08-25 / M2-1 / 결정: 휴지 조작은 넓은 영역의 아래 방향 스와이프로 확정했다. 위 방향은 무시하고, 대각선·좌우 흔들림을 보정하며, 거리와 속도에 비례한 가속을 적용한다.
- 2026-08-25 / M2-1 / 작업 발행: 휴지 진행률, 미션 중단·복귀, 일반·황금 휴지심 결과까지 한 화면의 상태 골격으로 구현하고 포인터 조작을 검증한다.
- 2026-08-25 / M2-1 / 구현: Pointer Events와 포인터 캡처로 넓은 한 손 조작 영역을 만들었다. 아래 방향 성분만 누적하고 1.5px 이하 떨림과 모든 위 방향 이동을 무시한다.
- 2026-08-25 / M2-1 / 구현: 이동 속도로 1.0~3.2배 가속을 계산해 진행량·롤 회전량·회전 전환 속도에 반영했다. 미션 체크포인트에서는 초과 진행을 잘라 비가역 진행률을 보존한다.
- 2026-08-25 / M2-1 / 구현: 일반 판 52%, 황금 판 36%·68%에서 미션으로 중단하고, 완료 후 같은 진행률에서 복귀해 100%에서 일반·황금 휴지심 결과로 전환한다.
- 2026-08-25 / M2-1 / 검증: `. .\scripts\activate-dev.ps1; npm test`에서 역방향·떨림 무시, 속도 가속, 상한·체크포인트 고정 테스트 3/3 통과.
- 2026-08-25 / M2-1 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. 70,945바이트 `sim-bwatta.ait` 생성.
- 2026-08-25 / M2-1 / 검증: 390×844 브라우저에서 대각선 아래 스와이프 후 11.33%, 역방향 위 스와이프 후에도 11.33% 유지. 52% 미션 정지·완료 후 복귀·100% 결과 도달 확인. 브라우저 경고·오류 0건.
- 2026-08-25 / M2-1 리뷰 / 완료: 한 손 조작, 비가역성, 부정확한 대각선 입력 보정, 속도 가속과 미션·결과 상태 전환이 명세·자동 테스트·모바일 브라우저 검증을 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-2 / 결정: 행동 미션은 진입 즉시 90초 가이드 타이머를 시작하되 조기 완료와 시간 초과를 허용한다.
- 2026-08-25 / M2-2 / 결정: 플레이 롤은 종이 원통 옆면이 주가 되는 휴지 걸이 구도로 바꾸고, 결과 휴지심은 문구가 원통 옆면에 적힌 형태로 바꾼다.
- 2026-08-25 / M2-2 / 작업 발행: 타이머 상태와 두 옆면 구도를 구현하고 390×844 모바일 화면 및 전체 루프를 검증한다.
- 2026-08-25 / M2-2 / 구현: 미션 진입 시 실제 시각 기준 90초 가이드 타이머를 시작하고 250ms마다 남은 시간을 갱신한다. 완료 버튼은 항상 활성화하고 0초 이후에도 실패시키지 않는다.
- 2026-08-25 / M2-2 / 구현: 플레이 롤을 폭은 유지하고 지름만 줄어드는 종이 원통 옆면으로 바꿨다. 종이 결 배경 이동으로 속도별 회전을 표현한다.
- 2026-08-25 / M2-2 / 구현: 결과 휴지심을 가로 원통 옆면으로 바꾸고 황당한 결과 문구를 옆면 내부에 배치했다. 일반·황금 재질을 분리했다.
- 2026-08-25 / M2-2 / 시각 수정: 첫 옆면안의 오른쪽 끝이 뾰족해 보여 본체를 직선 원통으로 바꾸고 끝 타원을 별도 요소로 분리했다.
- 2026-08-25 / M2-2 / 검증: `. .\scripts\activate-dev.ps1; npm test`에서 타이머 90초 시작·벽시계 감소·0초 하한·표시 형식과 기존 물리 테스트까지 6/6 통과.
- 2026-08-25 / M2-2 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. 71,905바이트 `sim-bwatta.ait` 생성.
- 2026-08-25 / M2-2 / 검증: 390×844 브라우저에서 미션 타이머 `1:30 → 1:29`, 조기 완료 후 복귀, 결과 문구의 휴지심 옆면 배치를 확인했다. 브라우저 경고·오류 0건.
- 2026-08-25 / M2-2 리뷰 / 완료: 90초 가이드·조기 완료·시간 초과 안전성, 플레이 롤 옆면, 결과 문구가 적힌 휴지심 옆면이 자동 테스트와 모바일 시각 검증을 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-3 / 변경: 하루 3회 제한과 날짜별 횟수 관리를 폐기하고 플레이 횟수를 제한하지 않는다.
- 2026-08-25 / M2-3 / 결정: 화면은 메인 다이어리와 플레이 두 개만 사용한다. 상세 정보·저장·공유는 메인의 바텀시트로 처리한다.
- 2026-08-25 / M2-3 / 결정: 진행 중 회차는 메모리에만 유지하고 앱 종료·WebView 재생성·새로고침 시 초기화한다. 영구 저장은 100% 완료 결과 생성 순간에만 수행한다.
- 2026-08-25 / M2-3 / 제안: 메인은 새 플레이 CTA, 총/황금 수 요약, 최신순 다이어리, 인라인 빈 상태, 로컬 저장 안내만 포함하고 나머지 부수 기능은 제출 범위에서 제외한다.
- 2026-08-25 / M2-3 / 확정: 메인 추천 구성을 그대로 채택했다. 새 플레이 CTA, 총/황금 수 요약, 최신순 단일 열 다이어리, 상세 바텀시트, 빈 상태와 로컬 저장 안내로 한정한다.
- 2026-08-25 / M2-3 / 작업 발행: 확정 메인 구성과 두 화면 전환을 만들고 완료 결과만 Storage에 저장하며 새로고침 시 진행 중 회차가 초기화되는지 검증한다.
- 2026-08-25 / M2-3 / 구현: 앱 진입점을 메인으로 바꾸고 총/황금 휴지심 요약, 최신순 단일 열 다이어리, 빈 상태, 고정 플레이 CTA, 상세 바텀시트를 구현했다.
- 2026-08-25 / M2-3 / 구현: 플레이 중 메인으로 이동하면 같은 실행 세션의 진행률·미션 상태를 유지하고 `플레이 계속하기`로 복귀한다. 앱 재실행·새로고침 후에는 진행 회차를 복원하지 않는다.
- 2026-08-25 / M2-3 / 구현: 휴지 진행률 100%에서 결과를 먼저 생성한 뒤 Apps in Toss `Storage`에 자동 저장한다. 결과 저장 성공 전에는 다이어리 복귀를 막고 실패 시 재시도한다. 일반 브라우저 개발 환경만 `localStorage`로 대체한다.
- 2026-08-25 / M2-3 / 검증: 저장 파서 복구·최신순 정렬·ID 중복 방지와 기존 타이머·물리 테스트까지 9/9 통과했다.
- 2026-08-25 / M2-3 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. 97,137바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-3 / 검증: 390×844 브라우저에서 29% 진행 상태의 메인 왕복 유지, 새로고침 시 진행 회차 초기화, 100% 완료 자동 저장, 재실행 후 다이어리 유지, 상세 바텀시트를 확인했다. 브라우저 오류 0건.
- 2026-08-25 / M2-3 리뷰 / 완료: 횟수 제한 제거, 두 화면 구조, 완료 시점 단일 저장, 진행 중 비저장 정책과 확정 메인 구성이 명세·자동 테스트·모바일 검증을 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-4 / 다음 작업: 다이어리 상세의 휴지심 결과 카드를 PNG로 생성하고 기기 저장과 공유 폴백을 연결한다.
- 2026-08-25 / M2-4 / 확인: SDK 3.1.1은 기존 `saveBase64Data`·`share`보다 `File.saveBase64`·`Share.sendMessage` 사용을 권장한다. 파일 저장은 Base64·파일명·MIME 타입을 받고, 공유는 메시지만 전달한다.
- 2026-08-25 / M2-4 / 작업 발행: Canvas 결과 카드를 PNG로 생성해 기기에 저장하고, 직접 이미지 첨부 대신 결과 문구·앱 링크를 네이티브 공유 시트로 전달한다. 개발 브라우저에는 다운로드·공유/복사 폴백을 둔다.
- 2026-08-25 / M2-4 / 구현: 일반·황금 재질, 휴지심 옆면 문구, 완료 미션과 날짜를 담은 1080×1350 Canvas PNG 카드를 만들었다. 픽셀 폭 기준 한글 줄바꿈과 안정적인 PNG 파일명을 분리해 테스트 가능하게 했다.
- 2026-08-25 / M2-4 / 구현: 앱에서는 `File.saveBase64`로 PNG를 저장하고 `Share.createLink`·`Share.sendMessage`로 결과 문구와 `intoss://sim-bwatta` 링크를 공유한다. 개발 브라우저에서는 Blob 다운로드와 클립보드 복사로 대체한다.
- 2026-08-25 / M2-4 / 구현: 다이어리 상세 바텀시트에 `이미지 저장`·`결과 공유` 버튼, 처리 중 비활성화, 성공·실패 피드백과 공유 방식 안내를 추가했다.
- 2026-08-25 / M2-4 / 검증: `. .\scripts\activate-dev.ps1; npm test`에서 카드 파일명·공유 메시지·한글 줄바꿈과 기존 저장·타이머·물리 테스트까지 12/12 통과했다.
- 2026-08-25 / M2-4 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. 99,130바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-4 / 검증: 390×844 브라우저에서 바텀시트 액션 배치, 공유 문구 복사, PNG 저장 피드백을 확인했다. 생성된 1080×1350 PNG 두 파일은 각 224,191바이트이며 문구·미션·휴지심 구도를 육안 확인했다. 브라우저 오류 0건.
- 2026-08-25 / M2-4 리뷰 / 완료: PNG 카드 생성과 개발 저장·공유 폴백은 자동·모바일 검증을 충족했고 프로덕션 SDK 호출은 타입 검사·빌드를 통과해 REVIEWED 처리했다. 네이티브 저장·공유 시트는 M2-5 샌드박스에서 최종 확인한다.
- 2026-08-25 / M2-5 / 다음 작업: Apps in Toss 샌드박스 실제 기기에서 전체 루프와 Storage·파일 저장·공유 시트를 통합 검증한다.
- 2026-08-25 / M2-UI-1 / 결정: 샌드박스 검증 전에 메인 다이어리를 한 줄의 풀린 휴지로 연결하고 휴지심 아이콘을 다듬는다. 플레이 롤은 실린더 형태, 동일한 휴지 폭, 넓은 칸 간격으로 보정한다.
- 2026-08-25 / M2-UI-1 / 결정: 플레이 롤 외경은 실제 새 롤 11cm와 휴지심 4cm 비율을 반영해 진행률 0%의 100%에서 100% 완료 시 약 36.4%까지 줄인다.
- 2026-08-25 / M2-UI-1 / 작업 발행: 사용자 지정 6개 시각 보정을 CSS와 진행률 계산에 반영하고 0%·중간·완료 직전 모바일 화면을 비교 검증한다.
- 2026-08-25 / M2-UI-1 / 구현: 플레이 롤의 중심축과 폭은 고정하고 외경만 진행률에 따라 132px에서 48px로 줄였다. 이는 11cm 대 4cm 비율이며, 범위 밖 진행률도 안전하게 보정한다.
- 2026-08-25 / M2-UI-1 / 구현: 플레이 롤 양 끝에 곡면과 고정 크기 구멍을 추가해 실린더로 만들었다. 아래 휴지는 롤 본체와 같은 206px 폭으로 맞추고 절취선 간격을 73px로 넓혔다.
- 2026-08-25 / M2-UI-1 / 구현: 최신순 기록 뒤에 한 장의 풀린 휴지 배경과 절취선을 배치해 다이어리를 연결했다. 빈 상태·일반·황금 휴지심 아이콘에 앞뒤 곡면, 테두리, 방사형 구멍을 추가했다.
- 2026-08-25 / M2-UI-1 / 검증: `. .\scripts\activate-dev.ps1; npm test`에서 11:4 외경 시작·종료·범위 보정과 기존 테스트까지 13/13 통과했다.
- 2026-08-25 / M2-UI-1 / 검증: `. .\scripts\activate-dev.ps1; npm run build` 성공. 99,523바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-1 / 검증: 390×844 브라우저에서 0%·29%·79% 롤 외경, 동일한 롤·휴지 폭, 넓어진 칸 간격을 비교했다. 일반·황금 기록 2개로 연결형 다이어리와 아이콘을 확인했고 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-1 리뷰 / 완료: 사용자 지정 6개 보정이 자동 테스트와 모바일 시각 비교를 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-2 / 변경: 플레이 롤은 앞으로 늘어진 휴지가 몸통을 덮고 오른쪽 단면·구멍·걸이 축이 드러나는 레퍼런스 구도로 다시 정의했다.
- 2026-08-25 / M2-UI-2 / 변경: 메인 아이콘과 완료 팝업 휴지심은 가로 원통에서 위쪽 타원면과 굽은 이음선이 보이는 세로 원통으로 바꾼다.
- 2026-08-25 / M2-UI-2 / 작업 발행: 레퍼런스의 방향과 겹침 관계를 CSS·마크업에 반영하고 모바일 메인·플레이·완료 팝업을 비교 검증한다.
- 2026-08-25 / M2-UI-2 / 구현: 플레이 휴지를 206px 동일 폭으로 맞추고 종이 앞면이 롤 몸통을 덮도록 겹쳤다. 오른쪽 단면은 앞에 남기고 구멍을 관통하는 축 조각을 추가했으며 찢긴 하단 실루엣을 적용했다.
- 2026-08-25 / M2-UI-2 / 구현: 빈 상태·다이어리 아이콘과 완료·상세 휴지심을 세로 원통으로 전환했다. 상단 타원, 둥근 하단, 몸통을 가로지르는 굽은 이음선과 일반·황금 재질을 반영했다.
- 2026-08-25 / M2-UI-2 / 검증: `. .\scripts\activate-dev.ps1; npm test` 13/13 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 99,649바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-2 / 검증: 390×844 브라우저에서 메인 빈 상태·플레이 0%·17%·52% 미션·100% 완료 팝업·완료 후 다이어리 목록을 확인했다. 브라우저 오류 0건.
- 2026-08-25 / M2-UI-2 리뷰 / 완료: 제공 레퍼런스의 플레이 겹침·방향과 세로 휴지심 실루엣이 모바일 렌더와 전체 루프에서 확인되어 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-3 / 변경: 메인 수집 요약의 두 라벨 옆에 일반·황금 휴지심 아이콘을 각각 배치한다.
- 2026-08-25 / M2-UI-3 / 변경: 플레이 오른쪽의 세로 고정가이드를 제거하고, 0% 롤 외경을 휴지 폭 206px와 같게 시작하되 11:4 축소 비율은 유지한다.
- 2026-08-25 / M2-UI-3 / 작업 발행: 세 가지 UI 보정만 구현하고 390×844 메인·0%·진행 중 플레이 화면과 빌드를 검증한다.
- 2026-08-25 / M2-UI-3 / 구현: 수집 요약 라벨 앞에 상단 타원·이음선을 축약한 일반·황금 세로 휴지심 아이콘을 배치하고 숫자 정렬을 유지했다.
- 2026-08-25 / M2-UI-3 / 구현: 플레이 오른쪽 세로 고정가이드 요소와 스타일을 제거했다. 초기 외경 기준을 132px에서 휴지 폭과 같은 206px로 변경해 종료 외경도 11:4 비율인 약 74.9px로 조정했다.
- 2026-08-25 / M2-UI-3 / 검증: `. .\scripts\activate-dev.ps1; npm test` 13/13 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 99,784바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-3 / 검증: 390×844 브라우저에서 일반·황금 요약 아이콘, 고정가이드 제거, 0% 롤 206×206px·휴지 폭 206px를 확인했다. 18%에서 폭 206px·외경 약 182.4px로 축소됐고 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-3 리뷰 / 완료: 요청한 세 가지 UI 보정이 실제 렌더·치수·자동 테스트·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-4 / 정정: M2-UI-3에서 제거 대상으로 오인한 오른쪽 세로 고정가이드를 복구한다. 실제 문제는 흰색 단면 타원의 존재가 아니라 롤 몸통과 단면 사이의 이격·접합이었다.
- 2026-08-25 / M2-UI-4 / 작업 발행: 레퍼런스처럼 몸통 위·아래 윤곽을 단면 타원의 접점에 연결하고 단면 구멍·축·고정가이드 중심을 정렬한 뒤 0%와 진행 중 화면을 검증한다.
- 2026-08-25 / M2-UI-4 / 구현: 오른쪽 세로 고정가이드 요소와 스타일을 복구했다. 단면 타원을 왼쪽으로 12px 이동해 롤 몸통 오른쪽 끝이 타원 중심과 만나도록 하고, 전면 축 시작점도 구멍 중심 안쪽으로 14px 이동했다.
- 2026-08-25 / M2-UI-4 / 검증: `. .\scripts\activate-dev.ps1; npm test` 13/13 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 99,866바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-4 / 검증: 390×844 브라우저의 0%·18%에서 몸통 오른쪽 끝과 단면 중심 X가 275.5px로 일치하고 롤·단면·축 중심 Y가 약 305px로 유지됨을 확인했다. 상단 이격이 사라졌고 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-4 리뷰 / 완료: 레퍼런스의 몸통·단면 접합과 고정가이드 복구가 실제 렌더·치수·자동 테스트·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-5 / 원인: `.paper-roll`의 `border-radius: 66px / 18px`가 네 모서리에 모두 적용돼 오른쪽 위·아래 윤곽이 단면 접점 전에 휘고 있었다. M2-UI-4의 위치 정렬만으로는 이 곡률이 남았다.
- 2026-08-25 / M2-UI-5 / 작업 발행: 롤 몸통과 내부 종이 결의 왼쪽 곡률만 유지하고 오른쪽 반지름을 0으로 바꿔 단면 타원까지 직선으로 연결되는지 검증한다.
- 2026-08-25 / M2-UI-5 / 구현: `.paper-roll`을 `66px 0 0 66px / 18px 0 0 18px`, 내부 `.paper-texture`를 같은 방향의 비대칭 곡률로 변경했다. 왼쪽 위·아래만 둥글고 오른쪽 위·아래는 직각이다.
- 2026-08-25 / M2-UI-5 / 검증: `. .\scripts\activate-dev.ps1; npm test` 13/13 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 99,882바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-5 / 검증: 390×844 브라우저에서 계산된 왼쪽 곡률 `66px 18px`, 오른쪽 위·아래 곡률 `0px`를 확인했다. 0%·18%에서 몸통 직선이 단면 접점까지 이어지고 몸통 끝·단면 중심 X가 275.5px로 일치했으며 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-5 리뷰 / 완료: 빨간 화살표 쪽 곡률 유지와 파란 화살표 쪽 직선 접합이 CSS 계산값·모바일 렌더·자동 테스트·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-6 / 변경: 휴지가 내려오기 시작하는 지점을 단면 상단이 아니라 축 중심 기준 정면 접점 `x=1`로 바꿔 화면상 축 높이에 고정한다.
- 2026-08-25 / M2-UI-6 / 원인: 기존 시작점은 `rollTop + rollDiameter × 0.19`여서 단면 상단 쪽에 있었고 외경 축소에 따라 이동했다. 회전값도 체크포인트에서 잘린 진행량이 아닌 원시 입력량으로 누적돼 휴지 연출보다 앞설 수 있었다.
- 2026-08-25 / M2-UI-6 / 작업 발행: 진행률에서 표면 이동량·롤 회전·휴지 길이·몸통/휴지 무늬 위치를 함께 계산하고, 정면 접선과 체크포인트 전후 동기화를 자동·모바일 검증한다.
- 2026-08-25 / M2-UI-6 / 구현: 휴지 시작점을 롤 축 높이 88px에 고정하고 외경 변화와 분리했다. 0~100% 진행을 표면 무늬 4칸(292px)·롤 4회전(1440도)·보이는 휴지 길이 170~300px에 공통 매핑했다.
- 2026-08-25 / M2-UI-6 / 구현: 원시 입력량을 별도로 누적하던 회전 상태를 제거했다. 몸통과 내려오는 휴지의 배경 위상을 같은 전역 좌표로 계산하고, 단면에 정면 접점에서 시작하는 미세 회전 표식을 추가해 속도별 CSS 전환 시간을 공유했다.
- 2026-08-25 / M2-UI-6 / 검증: `. .\scripts\activate-dev.ps1; npm test` 15/15 통과. 정면 접선 고정과 진행률 기반 동기화 테스트 2개를 추가했다. `. .\scripts\activate-dev.ps1; npm run build` 성공, 100,032바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-6 / 검증: 390×844 브라우저에서 0%·18.67%·36%의 롤 중심과 휴지 시작점이 약 305px로 고정됨을 확인했다. 36% 체크포인트에서 회전 518.4도, 휴지 길이 약 216.8px, 무늬 이동 105.12px가 같은 진행률에서 멈췄고 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-6 리뷰 / 완료: 정면 접선 시작점, 가속 입력에 따른 진행·회전 연출, 체크포인트 동기화가 계산 테스트·모바일 렌더·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-25 / M2-UI-7 / 변경: 시작 롤 직경·휴지 칸 간격·휴지심 외경을 사용자가 한 설정 파일에서 직접 조절할 수 있게 집약한다.
- 2026-08-25 / M2-UI-7 / 변경: 88~100%에서 흰 종이층을 걷어 갈색 휴지심을 드러내고, 바깥쪽 종이를 당기는 구도에 맞춰 오른쪽 단면 회전 표식을 반시계방향으로 바꾼다.
- 2026-08-25 / M2-UI-7 / 작업 발행: 시각 설정값을 TS·CSS 공통 변수로 연결하고 0%·99%·체크포인트에서 외경·종이 잔량·CCW 회전을 자동·모바일 검증한다.
- 2026-08-25 / M2-UI-7 / 구현: `src/game/rollVisualConfig.ts`에 시작 롤 외경 206px, 휴지 한 칸 73px, 휴지심 외경 75px를 집약하고 React 인라인 CSS 변수와 물리 계산이 같은 값을 사용하게 했다.
- 2026-08-25 / M2-UI-7 / 구현: 88~100% 구간에서 몸통·단면의 흰 종이층 투명도를 1에서 0으로 낮춰 갈색 휴지심을 드러냈다. 단면 회전값은 진행률 4회전을 유지하면서 음수 각도로 바꿔 반시계방향으로 동기화했다.
- 2026-08-25 / M2-UI-7 / 실패: 설정 파일을 확장자 없이 가져오자 Node 네이티브 TS 테스트가 모듈을 찾지 못했다. `.ts` 확장자 import와 `allowImportingTsExtensions`를 적용하고, 100% 외경 계산의 부동소수점 오차와 0%의 `-0` 회전값도 끝점 분기로 제거했다.
- 2026-08-25 / M2-UI-7 / 검증: `. .\scripts\activate-dev.ps1; npm test` 16/16 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 100,320바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-25 / M2-UI-7 / 검증: 390×844 브라우저에서 0% 외경 206px·칸 간격 73px·구멍 38px를 확인했다. 98.67%(표시 99%)에서 외경 76.75px, 흰 종이층 불투명도 0.111, 회전 -1420.8도를 확인했고 브라우저 오류는 0건이었다.
- 2026-08-25 / M2-UI-7 리뷰 / 완료: 직접 조정 가능한 세 시각값, 완료 직전 잔량 인지, 반시계방향 단면 회전이 계산 테스트·모바일 렌더·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-26 / M2-UI-8 / 변경: 사용자가 이미 조정한 시작 외경 153px·휴지심 외경 88px·구멍 41px은 보존한다. 전체 휴지 폭, 1~100 스와이프 가중치, 왼쪽 타원 깊이, 풀림 시작각을 `rollVisualConfig.ts`에 추가한다.
- 2026-08-26 / M2-UI-8 / 작업 발행: 가중치 50이 기존 조작량과 같고 1은 더 많은 스와이프, 100은 더 적은 스와이프가 되도록 연결한다. 왼쪽 윤곽은 오른쪽 단면의 반타원으로 보정하고 0도 정면 접점을 기본값으로 모바일 검증한다.
- 2026-08-26 / M2-UI-8 / 구현: `paperWidthPx`, `swipeWeight`, `leftEndCurveDepthPx`, `paperStartAngleDegrees`를 `rollVisualConfig.ts`에 추가했다. 폭은 본체·풀린 휴지·단면·앞축 위치에 공통 적용하고, 가중치 50=1배·100=2배·1≈0.51배의 연속 곡선으로 입력 진행량에 적용했다.
- 2026-08-26 / M2-UI-8 / 구현: 왼쪽 모서리의 기존 `66×18px` 얕은 곡률을 가로 깊이 23px·세로 반지름 `롤 외경/2`의 반타원으로 변경했다. 풀림 시작각은 -90~90도로 보정하며 0도는 기존 축 높이 정면 접점을 유지한다.
- 2026-08-26 / M2-UI-8 / 검증: `. .\scripts\activate-dev.ps1; npm test` 18/18 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 100,539바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-26 / M2-UI-8 / 검증: 390×844 브라우저에서 사용자가 조정한 외경 153px 기준 왼쪽 곡률 `23px / 76.5px`, 본체·풀린 휴지 폭 206px, 본체 오른쪽 끝과 단면 중심 일치를 확인했다. 브라우저 오류는 0건이었다.
- 2026-08-26 / M2-UI-8 리뷰 / 완료: 네 설정값이 한 파일에서 실제 물리·CSS에 반영되고 왼쪽 반타원·정면 접점이 모바일 렌더와 자동 테스트를 충족해 REVIEWED 처리했다.
- 2026-08-26 / M2-UI-9 / 변경: 100% 도달 즉시 결과 팝업이 뜨던 흐름에 완료 연출 상태를 추가한다. 입력을 잠그고 풀린 휴지가 휴지심에서 완전히 분리·낙하한 뒤 휴지심 팝 연출을 거쳐 결과 저장과 팝업으로 전환한다.
- 2026-08-26 / M2-UI-9 / 작업 발행: 완료 중복 실행과 타이머 누수를 막고, 100% 화면·중간 낙하·결과 팝업 순서를 모바일 브라우저에서 검증한다.
- 2026-08-26 / M2-UI-9 / 구현: `rolling`과 `result` 사이에 `finishing` 상태를 추가했다. 100% 도달 시 포인터·상단 이동을 잠그고 900ms 타이머가 끝난 뒤에만 결과 생성·Storage 저장·팝업 표시를 실행한다. 재시작·언마운트 시 타이머를 정리해 중복 완료를 막았다.
- 2026-08-26 / M2-UI-9 / 구현: 풀린 휴지는 아래 260px로 흔들리며 낙하·페이드하고, 갈색 휴지심은 밝아지며 단면 중심에서 일반/황금 링이 퍼진다. 완료 중 안내 문구는 `툭! / 마지막 휴지가 떨어졌어요`로 바꿨다. 시간·낙하 거리는 `rollVisualConfig.ts`에서 조절할 수 있다.
- 2026-08-26 / M2-UI-9 / 검증: `. .\scripts\activate-dev.ps1; npm test` 18/18 통과. `. .\scripts\activate-dev.ps1; npm run build` 성공, 101,030바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-26 / M2-UI-9 / 검증: 390×844 브라우저에서 100% 0ms·240ms·480ms·960ms를 연속 측정했다. 0~480ms에는 `finishing`·팝업 없음·휴지 110~142px 낙하·링 노출, 960ms에는 연출 종료 후 결과 팝업과 다이어리 저장을 확인했다. 브라우저 오류 0건.
- 2026-08-26 / M2-UI-9 리뷰 / 완료: 100% 도달과 결과 팝업 사이의 분리·낙하·휴지심 강조 연출이 상태 순서·저장·모바일 렌더·빌드를 충족해 REVIEWED 처리했다.
- 2026-08-26 / M2-REF-1 / 결정: `swipeWeight`는 이동거리 전체의 기본 감도, `swipeAcceleration`은 속도에 따른 추가 배율·상한으로 분리한다. 둘 다 1~100 범위와 50의 기존 기준값을 사용한다.
- 2026-08-26 / M2-REF-1 / 작업 발행: App 내부 콘텐츠 상수를 미션·휴지심 문구 파일로 각각 분리해 15개씩 확장하고 입력 물리·콘텐츠 개수·중복을 자동 검증한다.
- 2026-08-26 / M2-REF-1 / 구현: `swipeAcceleration` 1~100 설정을 추가했다. 50은 기존 속도 이득 1.35·상한 3.2를 유지하고, 값이 높을수록 속도 이득과 상한을 함께 높인다. `swipeWeight`의 이동거리 기본 감도와 독립적으로 적용한다.
- 2026-08-26 / M2-REF-1 / 구현: `MISSIONS`를 `src/game/missions.ts`, `CORE_MESSAGES`를 `src/game/coreMessages.ts`로 옮겼다. 약 90초 안에 가능한 작은 정리·호흡·스트레칭 미션과 귀여움·킹받음 톤의 보상 문구를 중복 없이 각각 15개로 확장했다.
- 2026-08-26 / M2-REF-1 / 실패: 읽기 전용 콘텐츠 배열을 기존 `pickRandom(T[])`에 넘겨 TypeScript 빌드가 실패했다. 콘텐츠의 불변성을 유지하고 `pickRandom(readonly T[])`로 입력 타입을 넓혀 해결했다.
- 2026-08-26 / M2-REF-1 / 검증: `. .\scripts\activate-dev.ps1; npm test` 21/21 통과. 가속도 1·50·100 순서와 두 콘텐츠의 15개·ID/문구 중복·빈 값 검사를 추가했다. `. .\scripts\activate-dev.ps1; npm run build` 성공, 101,941바이트 `sim-bwatta.ait`를 생성했다.
- 2026-08-26 / M2-REF-1 리뷰 / 완료: 입력 감도 두 축의 독립성, 콘텐츠 모듈 경계와 15개 바리에이션이 자동 테스트·타입 검사·번들을 충족해 REVIEWED 처리했다.
- 2026-08-26 / M2-5 / 확인: 로컬 Apps in Toss CLI 3.1.1은 `ait build`와 토큰·워크스페이스 기반 `ait deploy`를 제공한다. 번들 생성까지 완료했으며 외부 배포와 실제 네이티브 브리지 검증은 콘솔·실기기 접근이 필요한 단계다.
- 2026-08-26 / M2-5 / 확인: 앱인토스 콘솔 가입과 워크스페이스 생성을 완료했다. 공식 문서상 워크스페이스명은 팀·프로젝트를 식별하는 내부 협업용이며 Google Play 개발자명과 별개이고, 나중에 수정할 수 있다.
- 2026-08-26 / M2-5 / 확인: 콘솔 앱 등록값은 앱 이름 `심봤다!`, 수정 불가 appName `sim-bwatta`, 유형 `게임`으로 확정했다. 챌린지는 앱 정보 승인 전에도 첫 번들 등록을 허용하므로 마감 우선순위를 앱 생성 → 번들 등록 → 출품 신청으로 둔다.
- 2026-08-26 / M2-5 / 제안: 게임 카테고리는 `인디`를 권장한다. 현실 시스템을 지속적으로 모사하는 시뮬레이션보다 짧고 독특한 조작 루프·랜덤 수집 보상 중심의 소규모 게임이라는 성격에 더 가깝다.
- 2026-08-26 / M2-5 / 구현: `docs/assets/store`에 라이트·다크 앱 로고, 썸네일, 실제 앱 기반 세로 스크린샷 4장과 재생성 스크립트·업로드 안내를 만들었다. 캡처 전용 Vite 설정은 SDK mock을 유지하고 개발 패널만 제외한다.
- 2026-08-26 / M2-5 / 검증: 앱 로고 2종은 600×600, 썸네일은 1932×828, 스크린샷 4종은 636×1048의 불투명 PNG임을 자동 검사하고 전 파일을 시각 검수했다.
- 2026-08-26 / M2-5 / 보완: 실제 플레이·미션·결과 화면을 한 장에 구성한 1504×741 가로형 스크린샷을 재생성 스크립트와 업로드 안내에 추가했다.
- 2026-08-26 / M2-5 / 검증: 가로형 스크린샷이 1504×741 RGB PNG이며 세 화면과 제목·단계 라벨에 잘림이 없음을 자동 검사와 원본 크기 시각 검수로 확인했다.
- 2026-08-26 / M2-5 / 작업 발행: `sim-bwatta.ait`를 테스트 배포에 등록하고 실제 기기에서 전체 루프, 재실행 저장 정책, `File.saveBase64`, `Share.sendMessage`를 체크리스트 순서로 확인한다.
- 2026-08-26 / M2-5 / 블로커 확인: 게임 등급·스토어 정보가 없어 앱 정보 검토를 완료할 수 없지만, 공식 챌린지 안내상 앱 정보 승인 전에도 첫 번들 등록과 출품 신청이 가능하므로 8월 26일 제출 블로커는 아니다.
- 2026-08-26 / M2-5 / 확인: 앱인토스는 개인 개발자의 비사업자 출시를 허용한다. 사업자가 없으면 토스 로그인·결제·광고 등 수익화 기능이 제한되지만 현재 MVP는 해당 기능을 사용하지 않는다.
- 2026-08-26 / M2-5 / 주의: 기존에 출시한 다른 게임의 스토어 링크·등급번호를 재사용하거나 비게임으로 오분류하지 않는다. 정식 출시 전 동일 게임의 스토어 등급 또는 게임물관리위원회 증명서를 별도로 확보한다.
- 2026-08-26 / M2-5 / 확인: 개인용 계정으로 만든 워크스페이스는 개발용 계정을 `관리자`로 초대한 뒤 대표관리자 권한을 위임할 수 있다. 앱·appName을 새로 만들거나 다른 워크스페이스로 복제할 필요가 없다.
- 2026-08-26 / M2-5 / 제안: 마감 전에는 기존 대표관리자를 유지하고 개발용 계정을 관리자에 추가하는 데 그친다. 출품폼과 첫 번들 등록을 완료한 뒤 권한 위임·기존 계정 정리를 진행한다.
- 2026-08-26 / M2-5 / 우선순위 확정: 스토어·게임 등급 설정을 보류하고 최신 `.ait` 첫 번들 등록을 먼저 완료한다.
- 2026-08-26 / M2-5 / 원인: 기존 101,941바이트 번들은 00:48 생성 후 `src/App.tsx`가 02:10에 수정되어 최신 소스보다 오래된 상태였다. 업로드 전 재빌드가 필요하다고 판단했다.
- 2026-08-26 / M2-5 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`를 실행해 테스트 21/21, TypeScript·Vite·`ait build`를 통과했다. 02:37 생성된 101,941바이트 `sim-bwatta.ait`가 모든 앱 소스보다 최신이며 appName `sim-bwatta`와 일치한다.
- 2026-08-26 / M2-5 / 완료: 02:40에 `sim-bwatta.ait`를 번들 버전 `20260826-1`, SDK 3.1.1로 콘솔에 등록했다. 상태 `검토 필요`는 앱 검토를 아직 요청하지 않은 상태이며, 챌린지의 첫 번들 등록 조건은 충족했다.
- 2026-08-26 / M2-5 / 다음 작업: `검토 요청`은 게임 등급 정보 확보 전까지 보류한다. 먼저 `테스트`에서 실제 기기 진입과 핵심 루프를 확인한 뒤 출품폼을 제출한다.
- 2026-08-26 / M2-5 / 확인: 번들 `20260826-1`의 테스트 모달이 정상 열렸고 QR 딥링크와 워크스페이스 멤버 대상 테스트 푸시 전송 수단이 제공됐다. 선택된 멤버의 토스 앱에서 실기기 실행을 확인하는 단계다.
- 2026-08-26 / M2-5 리뷰 / 완료: 사용자가 토스 앱 실기기에서 핵심 루프와 Storage·이미지 저장·공유 기능을 확인했고 기능상 문제 없음을 보고했다. M2 완료 조건을 충족해 REVIEWED 처리했다.
- 2026-08-26 / M3-QA-1 / QA 접수: 황금 휴지심 체감 빈도, 누적 목록의 상단 공간 활용, `다이어리` 명칭과 사용자 커스텀 부재, 고정 로컬 저장 안내의 UI 겹침을 개선 대상으로 기록했다.
- 2026-08-26 / M3-QA-1 / 작업 발행: 먼저 황금 휴지심 확률을 `rollVisualConfig`의 0~100 값으로 설정화하고, 기록이 쌓인 메인 화면에서 발견/황금 요약을 sticky 오버레이로 보정한다.
- 2026-08-26 / M3-QA-1 / 구현: `rollVisualConfig.ts`에 `goldenCoreChancePercent`를 추가했다. 0~100을 확률 0~1로 보정해 라운드 생성에 사용하며 기본값 30은 기존 황금 판·두 번째 미션 동작을 보존한다.
- 2026-08-26 / M3-QA-1 / 구현: 발견/황금 휴지심 요약을 `position: sticky`와 상단 8px, 반투명 배경·14px 블러·z-index 7로 구성해 목록 스크롤 중에도 상단 오버레이로 유지했다.
- 2026-08-26 / M3-QA-1 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`를 실행해 테스트 22/22와 TypeScript·Vite·`ait build`를 통과했다. 새 `sim-bwatta.ait`는 102,039바이트다.
- 2026-08-26 / M3-QA-1 / 시각 검증: 390×500 모바일 뷰에서 스크롤 후 요약 영역의 계산값이 `position: sticky`, `top: 8px`, `z-index: 7`, `blur(14px)`이며 보관 목록 위에 고정되는 것을 확인했다.
- 2026-08-26 / M3-QA-2 / 결정: 사용자 작성 메모는 제출 범위에 추가하지 않고, 사용자 노출 `다이어리` 명칭을 `휴지심 보관함`으로 바꾼다. 로컬 저장 안내는 삭제하지 않고 고정 CTA에서 보관함 제목 아래의 비고정 보조 문구로 옮긴다.
- 2026-08-26 / M3-QA-2 / 사용자 변경 보존: `rollVisualConfig`의 `swipeWeight: 55`, `swipeAcceleration: 87`, `goldenCoreChancePercent: 13`을 현재 튜닝값으로 확인했다. 이 값을 덮어쓰지 않고 문서의 확률·기대 시간만 13% 기준으로 갱신한다.
- 2026-08-26 / M3-QA-2 / 작업 발행: 사용자에게 노출되는 메인·플레이·완료 팝업·결과 카드의 `다이어리` 표현을 보관함으로 통일하고, 기존 Storage 키와 내부 타입명은 기록 호환성을 위해 유지한다.
- 2026-08-26 / M3-QA-2 / 구현: 메인 헤더·섹션·로딩 상태, 플레이 복귀 버튼, 완료 저장 상태·복귀 버튼, PNG 결과 카드의 사용자 노출 명칭을 `휴지심 보관함`으로 통일했다. `DiaryEntry`와 `sim-bwatta:diary:v1`은 기존 기록 호환성을 위해 변경하지 않았다.
- 2026-08-26 / M3-QA-2 / 구현: `보관함 기록은 이 기기에만 저장돼요.`를 보관함 제목 아래 비고정 보조 문구로 옮기고, 고정 하단은 `새 휴지 풀기` CTA만 남겼다. 하단 안전 여백도 122px에서 104px로 줄였다.
- 2026-08-26 / M3-QA-2 / 실패와 해결: 사용자가 `swipeAcceleration`을 87로 변경하면서 기본 설정을 암묵적으로 사용한 가속 상한 테스트가 기존 50 기준값 3.2와 달라졌다. 테스트 입력에 기준 설정 50을 명시해 현재 사용자 튜닝과 물리 기준 검증을 분리했다.
- 2026-08-26 / M3-QA-2 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`를 다시 실행해 테스트 22/22, TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 102,050바이트이며 사용자 설정 55/87/13이 보존됐다.
- 2026-08-26 / M3-QA-2 / 시각 검증: 390×844에서 `휴지심 보관함` 제목과 비고정 저장 안내, CTA 단독 고정 상태를 확인했다. 390×500 스크롤에서 요약 `top: 8px`, 안내 하단 134px, CTA 상단 422px로 겹침이 없었다.
- 2026-08-26 / M3-QA-2 리뷰 / 완료: 사용자가 PC에서 요청한 보관함 명칭·저장 안내·sticky 통계를 확인하고 의도대로 수정됐다고 승인했다. 최신 QA 번들은 아직 콘솔 테스트 배포하지 않았으며 실기기 QA는 이후 진행한다.
- 2026-08-26 / M3-QA-3 / 원인: `.paper-sheet`의 오른쪽 끝이 `paper-roll` 끝과 같은 X, 즉 46px 단면 타원의 중심선에 놓여 단면 하단 오른쪽 절반이 종이 밖으로 돌출돼 보였다.
- 2026-08-26 / M3-QA-3 / 작업 발행: 풀린 휴지의 오른쪽 끝을 단면 타원의 왼쪽 접점에 맞추고, 진행률 변화 중에도 접점에서 수직선으로 이어지는지 PC 모바일 뷰에서 검증한다.
- 2026-08-26 / M3-QA-3 / 구현: 오른쪽 단면 폭 46px을 `rollEndWidthPx`와 CSS 변수로 공유했다. 풀린 휴지 폭은 전체 롤 폭에서 단면 반폭 23px을 제외해 오른쪽 끝이 단면 타원의 왼쪽 접점에 놓이도록 했다.
- 2026-08-26 / M3-QA-3 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`를 실행해 테스트 22/22, TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 102,080바이트다.
- 2026-08-26 / M3-QA-3 / PC 시각 검증: 390×844에서 단면 폭 46px, 롤 폭 206px, 풀린 휴지 폭 183px을 확인했다. 0%와 7.31% 모두 휴지 오른쪽 X와 단면 왼쪽 X가 252.5px로 같아 차이 0px을 유지했다.
- 2026-08-26 / M3-QA-4 / 원인: 기존 연출은 전체 플레이에서 휴지 절취선이 4칸만 지나가고 단면도 4회전해, 빠른 스와이프의 입력 가속에 비해 칸이 거의 고정된 것처럼 보였다. 절취선·롤 회전·보이는 길이를 모두 한 이동량으로 묶은 구조가 속도 표현을 제한했다.
- 2026-08-26 / M3-QA-4 / 작업 발행: 실제 진행률이라는 단일 기준은 유지하면서 전체 칸 이동 수와 단면 회전 수를 별도 설정으로 분리하고, 스와이프 속도에 비례한 짧은 절취선 잔상을 추가해 PC에서 느린·빠른 입력을 비교 검증한다.
- 2026-08-26 / M3-QA-4 / 구현: `rollVisualConfig`에 `visualPanelPasses: 18`, `visualRollTurns: 7`, `motionTrailMaxOpacity: 0.22`를 추가했다. 휴지 칸 이동량과 반시계 단면 회전량은 각각 실제 진행률에서 계산하고, 절취선 이동 전환은 선형으로 바꿔 연속 입력 중의 멈칫거림을 줄였다.
- 2026-08-26 / M3-QA-4 / 구현: 몸통과 풀린 휴지에 속도 비례 절취선 잔상을 추가했다. 입력이 멈추면 180ms 뒤 잔상이 0으로 사라지고, 반복 무늬는 실제 진행률의 목표 위치에 정렬된다.
- 2026-08-26 / M3-QA-4 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`를 실행해 테스트 23/23, TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 102,260바이트다.
- 2026-08-26 / M3-QA-4 / PC 시각 검증: 390×844에서 빠른 145px 스와이프는 진행률 15.13%, 절취선 약 2.72칸 이동, 단면 약 1.06회전, 전환 70ms·잔상 0.22로 측정됐다. 느린 54px 스와이프는 전환 181.25ms·잔상 0.103이었다. 정지 후 잔상 0, 절취선 목표 위치 일치, 휴지 오른쪽·단면 왼쪽 접점 차이 0px, 브라우저 경고·오류 0건을 확인했다.
- 2026-08-26 / M3-QA-5 / 결정: 롤 풀림 속도감은 반복 무늬뿐 아니라 휴지 밑단의 실제 하강으로 표현한다. 휴지 높이는 절취선 이동량과 1:1로 늘리고 플레이 카드에서 잘리게 하며, `↓ 아래로` 안내는 별도 레이어로 분리해 기존 이동 범위만 유지한다.
- 2026-08-26 / M3-QA-5 / 작업 발행: 휴지 밑단을 조작 안내 뒤로 통과시키고 빠른·느린 스와이프에서 높이 변화가 같은 전환 시간으로 동기화되는지, 단면 접점과 완료 연출이 유지되는지 PC 모바일 뷰에서 검증한다.
- 2026-08-26 / M3-QA-5 / 구현: 실제 `.paper-sheet` 높이를 `초기 170px + paperTravel`로 변경해 절취선 이동과 밑단 하강을 1:1로 맞췄다. 높이 전환도 절취선과 같은 `spinDuration` 선형 전환을 사용한다.
- 2026-08-26 / M3-QA-5 / 구현: `↓ 아래로` 문구를 `.paper-guide` 독립 레이어로 옮겨 기존 170~300px 이동 범위를 보존했다. 실제 휴지는 z-index 3, 조작 안내는 z-index 7로 두고 플레이 카드의 `overflow: hidden`을 이용해 휴지가 안내 뒤로 지나간 뒤 카드 하단에서 잘리게 했다. 완료 진입 시 문구만 제자리에서 페이드한다.
- 2026-08-26 / M3-QA-5 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 23/23, TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 102,348바이트다.
- 2026-08-26 / M3-QA-5 / PC 시각 검증: 390×844의 30.27%에서 실제 휴지는 567.72px, 안내 레이어는 209.34px이었다. 휴지 밑단 872.72px은 조작 안내 상단 732px과 플레이 카드 하단 825px을 모두 지나 z-index 3/7 관계로 뒤에서 잘렸다. 52% 미션 정지와 100% 분리·결과 저장까지 확인했고 브라우저 경고·오류는 0건이었다.
- 2026-08-26 / M3-QA-6 / 확정: 돌발 휴지 칸은 첫 완료 기록이 없는 판에는 보장한다. 이후 직전 완료 판에 등장했다면 다음 한 판은 제외하고, 그 외에는 35% 확률로 생성한다. 콘텐츠와 안전 구간의 위치는 라운드 생성 때 한 번 무작위로 정한다.
- 2026-08-26 / M3-QA-6 / 저장 정책: 완료 기록의 선택 필드로 돌발 발생 여부만 남긴다. 진행 도중에는 저장하지 않으므로 첫 판에서 돌발 칸을 본 뒤 종료해도 다음 시도에 다시 첫 판 보장을 적용한다.
- 2026-08-26 / M3-QA-6 / 작업 발행: CSS 반복 배경은 유지하고 한 개의 돌발 콘텐츠 레이어만 휴지 이동량에 동기화한다. 첫 판·쿨다운·35% 경계, 구버전 기록 호환, 안전 위치, 빠른 스와이프 통과와 100% 완료 회귀를 자동·PC 검증한다.
- 2026-08-26 / M3-QA-6 / 구현: `surprisePanels.ts`에 `꽝`, `봤지?`, `왜?`, `아님`, `비밀`, `검열`, 눈, 발자국, `한 칸 더`, `텅` 10종과 안전 패널 인덱스 4·5·6·13·14·15를 분리했다. 첫 판은 확률 계산을 건너뛰어 보장하고, 이후 최근 완료 기록의 쿨다운과 35% 경계를 적용한다.
- 2026-08-26 / M3-QA-6 / 구현: 라운드에 단 하나의 돌발 계획을 고정하고, 해당 패널의 위치를 `paperOffset`과 같은 70~230ms 선형 전환으로 이동시켰다. 텍스트·붉은 도장·CSS 눈·발자국을 휴지 안에 렌더하며 팝업이나 진행 정지는 추가하지 않았다.
- 2026-08-26 / M3-QA-6 / 구현: `DiaryEntry.hadSurprise?`를 선택 필드로 추가해 구버전 기록을 유지하고 완료 때만 저장한다. 기록 로딩 전 새 판 시작을 잠가 첫 판 판정의 비동기 경합을 제거했다.
- 2026-08-26 / M3-QA-6 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 첫 판 보장·직전 판 쿨다운·35% 경계·콘텐츠/슬롯 중복·구버전 기록 호환을 포함한 테스트 27/27과 TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 103,265바이트다.
- 2026-08-26 / M3-QA-6 / PC 시각 검증: 기록 0개인 `localhost` 390×844 첫 판에서 `봤지?`·패널 6이 보장됐다. 30.27%에서 47.73px가 빠른 70ms 전환으로 진입하고 34.44%에서 한 칸 전체가 렌더됐다. 52% 미션·100% 완료 저장 뒤 보관함 1개를 확인했으며, 즉시 시작한 다음 판은 돌발 요소 0개로 쿨다운됐다. 브라우저 경고·오류는 0건이었다.
- 2026-08-26 / M3-QA-7 / 변경: PC에서 최초 플레이를 반복 검증할 수 있도록 개발 빌드에만 완료 기록 초기화 버튼을 둔다. 초기화 후 다음 판은 첫 판 돌발 보장을 다시 적용한다.
- 2026-08-26 / M3-QA-7 / 원인: 절취선은 휴지 배경 위치, 돌발 칸은 별도 `top` 전환으로 움직여 같은 시간값이어도 서로 다른 렌더 경로에서 속도 괴리가 보였다.
- 2026-08-26 / M3-QA-7 / 작업 발행: 절취선과 돌발 칸을 하나의 transform 이동 트랙에 배치해 위치·전환을 완전히 공유하고, 개발 초기화 UI·기존 저장 정책·완료 흐름의 회귀를 검증한다.
- 2026-08-26 / M3-QA-7 / 구현: 메인 보관함에 `PC 테스트 데이터 초기화` 버튼을 개발 모드에서만 추가했다. 누르면 `sim-bwatta:diary:v1` 완료 기록과 현재 회차 상태를 비워 다음 판을 최초 플레이로 다시 판정하며 성공 문구를 표시한다.
- 2026-08-26 / M3-QA-7 / 구현: 풀린 휴지의 절취선·속도 잔상·돌발 칸을 `.paper-motion-track` 자식으로 합쳤다. 세 요소는 `paperOffset`에서 계산한 하나의 `translate3d`와 같은 선형 전환을 상속하며 돌발 칸의 별도 `top` 전환은 제거했다.
- 2026-08-26 / M3-QA-7 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 27/27과 TypeScript·Vite·`ait build`를 통과했다. 최신 `sim-bwatta.ait`는 103,429바이트이며 `dist`에 개발 초기화 문구가 없어 배포 빌드 제외를 확인했다.
- 2026-08-26 / M3-QA-7 / PC 검증: 메인에서 개발 초기화 버튼 1개가 보이고 활성화된 상태임을 확인했다. 플레이의 이동 트랙은 1개, 계산 스타일은 `transition-property: transform`, `linear`, 시작 transform 0px였으며 현재 쿨다운 판에는 돌발 칸이 생성되지 않았다. 기존 테스트 데이터 삭제 동작은 실행하지 않았다.
- 2026-08-26 / M3-QA 종료 / 사용자 승인: 사용자가 PC에서 최초 플레이 초기화와 돌발 칸 동기화를 포함한 현재 동작을 직접 확인했고, 문제 없이 의도대로 작동한다고 승인했다. M3-QA-1~7을 REVIEWED, 제출 준비 M3를 DONE으로 종료한다.
- 2026-08-26 / M4-1 / 버전 결정: 앱 버전은 `0.2.1`, 표기 규칙은 Semantic Versioning `MAJOR.MINOR.PATCH`로 확정했다. 일반 배포와 hotfix 모두 PATCH를 올리며 예시는 `0.5.3 → 0.5.4`다.
- 2026-08-26 / M4-1 / 작업 발행: `package.json`과 `package-lock.json`의 루트 버전을 0.2.1로 맞추고 전체 테스트·빌드 후 `.ait`의 `project-package.json`까지 0.2.1인지 검증한다.
- 2026-08-26 / M4-1 / 구현: `package.json`, `package-lock.json` 최상위, lockfile 루트 패키지 버전을 모두 `0.2.1`로 변경했다.
- 2026-08-26 / M4-1 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 27/27과 TypeScript·Vite·`ait build`를 통과했다. `.ait`의 `project-package.json`도 0.2.1이며 appName `sim-bwatta`, SDK 3.1.1을 확인했다.
- 2026-08-26 / M4-1 / 산출물: `sim-bwatta.ait` 103,427바이트, deploymentId `01a03a7a-20f2-7a62-b0b5-05b84dff57d6`, SHA-256 `FCA6FA1879FCF69B865D558868C2079328B79B3AAD7EA09ECAE3FA4E43D29811`.
- 2026-08-26 / M4-1 / 검증 보완: 최초 lockfile 확인에서 PowerShell `ConvertFrom-Json`이 빈 문자열 키를 거부했다. `-AsHashtable`로 다시 읽어 최상위·루트 패키지 버전이 모두 0.2.1임을 확인했다.
- 2026-08-26 / M4-1 / 버전 규칙 정정: 기존 `[major].[minor].[deploy][hotfix]`와 `0.5.3d` 표기는 npm SemVer가 아니므로 폐기했다. 정식 출시 전 불안정 버전이라는 의미의 현재 MAJOR 0은 유지하고, 기능 호환 변화는 MINOR, 배포 수정과 hotfix는 PATCH 증가로 통일한다.
- 2026-08-26 / M4-2 / 사용자 진행: 검증된 0.2.1 `sim-bwatta.ait`를 앱인토스 콘솔에 업로드했다. 챌린지 출품 신청은 아직 하지 않았다.
- 2026-08-26 / M4-2 / 공식 확인: 챌린지 페이지의 참여 방법은 콘솔 밖의 `내 미니앱 출품하기` 신청폼 작성과 8월 26일까지 콘솔 앱 번들 등록을 별도 단계로 안내한다. 따라서 콘솔에 챌린지 표기가 없는 것은 정상이며 `.ait` 업로드만으로 출품되지 않는다.
- 2026-08-26 / M4-2 / 신청 항목: 별도 폼에 이름·이메일·전화번호, 한국어 앱 이름, appName, 미니앱 한줄 소개, 주제 연관성 200자 이내, 필수 개인정보 동의를 입력해야 한다. 신청폼 appName은 콘솔 값 `sim-bwatta`와 정확히 같아야 한다.
- 2026-08-26 / M4-2 / 이름 정정: 콘솔이 느낌표를 허용하지 않아 공식 앱 이름은 `심봤다`로 등록했다. 인앱 타이틀 `심봤다!`는 스타일 표기로 구분한다.
- 2026-08-26 / M4-2 / 제출 완료: 사용자가 공식 앱 이름 `심봤다`, appName `sim-bwatta`로 챌린지 별도 신청폼 제출을 완료했다. 0.2.1 번들 업로드와 신청폼 조건을 모두 충족해 M4를 DONE으로 종료한다.
- 2026-08-26 / M5 제안 / 문제: 단순한 핵심 루프를 최초 여러 판에 연속 소비하면 빠르게 질릴 수 있다. 플레이 차단만 추가하면 불쾌한 제한이 되므로, 예측 가능한 리필 시각·남은 시간·기존 보관함 이용을 함께 제공해 다음 방문 약속으로 표현해야 한다.
- 2026-08-26 / M5 제안 / 추천: 하루 3롤을 고정 시각에 전량 리필한다. 완료 시에만 1롤을 소비하고 중도 종료는 소비하지 않는다. 고정 시각은 완료 후 개별 쿨다운보다 일상 속 같은 시점의 재방문 습관과 정기 푸시 확장에 적합하다.
- 2026-08-26 / M5 제안 / 푸시 제약: 앱인토스는 비사업자도 푸시·알림을 사용할 수 있지만 기능성 푸시는 토스 로그인 연동이 필요하다. 사용자별 리필 시점 API 발송은 서버가 필요하며, 재방문 유도 문구는 광고성으로 판단될 수 있어 메시지 유형·동의문 검토가 선행돼야 한다.
- 2026-08-26 / M5 제안 / 로드맵: 리필 루프 → 수집 도감·희귀도 → 사용자 메모 다이어리 → 로그인·푸시 순서로 확장한다. 제한이 먼저 들어가더라도 보관함·수집이라는 돌아올 이유를 함께 강화한다.
- 2026-08-26 / M5 수정 / 사용자 검증: 행동 미션 가이드라인을 최대로 사용해도 실제 한 회차가 2분 미만이었다. 3회를 한 자리에서 소진한 뒤 다음 날까지 기다리게 하는 하루 3롤 전량 리필안은 의도와 어긋나므로 폐기한다.
- 2026-08-26 / M5 수정 / 방향: 최초 2회는 연속 플레이를 허용하고 이후 완료 회차부터 단계형 쿨다운을 둔다. 현재 추천값은 `30분 → 2시간 → 6시간`, 6시간 상한, 현지 시각 06:00 단계 초기화다. 중도 종료는 단계에 반영하지 않는다.
- 2026-08-26 / M5 수정 / UX: 제한 중 보관함은 계속 이용할 수 있으며 새 플레이 영역에 `두루마리 휴지를 리필하고 있어요`, 절대 해제 시각, 남은 시간을 함께 표시한다.
- 2026-08-26 / 버전 로드맵: 이번 제한 업데이트는 `0.3.0`에 한정한다. 이후 `0.4.0` 수집 도감·콘텐츠, `0.5.0` 수집 연계 리워드, `0.6.0` 사용자 메모 다이어리, `0.7.0` 이상 로그인·푸시·서버 연동으로 기능 경계를 나눈다. 같은 범위의 수정은 PATCH를 올린다.
- 2026-08-26 / M5 수정 / 간격 확정: 첫 2회는 연속 플레이를 허용하고, 2회 완료 후 `5분`, 3회 완료 후 `30분`, 4회 완료 후 `2시간`을 적용한다. 기존 6시간 상한은 길다는 판단에 따라 `4시간 30분~5시간`으로 재검토하며, 인지하기 쉬운 5시간을 추천한다.
- 2026-08-26 / M5 수정 / 목표 용어: 보편적인 습관 형성 기간을 주장하지 않고, 제품에서 측정할 초기 온보딩 목표를 `설치 후 3일 연속 재방문`으로 정의한다.
- 2026-08-26 / M5-1 / 설계 확정: 최초 2회 연속 플레이 후 `5분 → 30분 → 2시간 → 5시간`을 적용하고 5시간을 상한으로 반복한다. 단계는 현지 시각 06:00에 초기화하며 완료 회차만 진행시킨다.
- 2026-08-26 / M5 전략 / 판단: 쿨다운은 콘텐츠 과소비 방지용 페이싱 장치이며 그 자체로 돌아올 이유가 되지는 않는다. 장기적으로 첫날 핵심 이해, 둘째 날 새 콘텐츠 기대, 셋째 날 첫 수집 이정표를 제공하는 3일 온보딩 아크를 콘텐츠 버전에서 검토한다.
- 2026-08-26 / M5 전략 / 스트릭: `3일 연속 재방문`은 우선 내부 성과 지표로 사용한다. 하루 누락을 실패로 강조하는 스트릭 UI는 초기화 손실감을 만들 수 있으므로 별도 검증 전에는 사용자에게 노출하지 않는다.
- 2026-08-26 / M5 전략 / 측정: 출시 후 단계별 쿨다운 해제 복귀율, 설치 다음 날·그다음 날 복귀율, 복귀 후 완료율을 본다. 앱인토스 라이브 환경의 이벤트 분석 도입 여부는 구현 범위 확정 때 결정한다.
- 2026-08-26 / M5-2 / 계측 확정: 사용자 승인에 따라 `0.3.0` 제한 업데이트에 회차 시작·완료, 쿨다운 노출, 해제 후 복귀 최소 이벤트를 포함한다. 콘텐츠 원문은 보내지 않고 계측 실패는 플레이를 막지 않는다.
- 2026-08-26 / M5-2 / 작업 발행: 완료 기록에서 현지 시각 06:00 기준 당일 완료 수와 해제 시각을 계산하고, 제한 CTA·카운트다운·절대 시각, 완료 시에만 단계 진행, 개발 초기화·구버전 기록 호환, Analytics 이벤트를 한 작업으로 구현·검증한다.
- 2026-08-26 / M5-2 / 구현: `refillSchedule.ts`에서 완료 기록의 ISO 시각을 현지 시각 06:00 기준 제품 일자로 묶고, 당일 완료 수에 따라 `0분 → 5분 → 30분 → 2시간 → 5시간`을 계산한다. 쿨다운이 다음 06:00을 넘으면 초기화 시각을 해제 상한으로 사용한다.
- 2026-08-26 / M5-2 / 구현: 메인 하단에 `두루마리 휴지를 리필하고 있어요`, 절대 해제 시각, 초 단위 남은 시간을 표시하고 새 플레이 CTA를 잠근다. 기존 활성 회차는 계속할 수 있고 완료 저장이 성공한 뒤에만 새 단계가 반영된다.
- 2026-08-26 / M5-2 / 계측: `Analytics.log`로 `sim_bwatta_roll_start`, `roll_complete`, `refill_locked`, `refill_return` 이벤트를 추가했다. 익명키는 SDK 기본값을 사용하고 콘텐츠 원문은 제외했으며 전송 실패는 무시한다.
- 2026-08-26 / M5-2 / 자동 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 전체 테스트 35/35와 TypeScript·Vite·`ait build`를 통과했다. 0.3.0 번들은 104,782바이트, deploymentId `01a03abf-bd58-7a37-85cc-cfd03e92d4a4`, SHA-256 `B09AECE50EF9953E3DD669C5817AE2D2B32C91D075D04845D3ABDC5F405DB2E7`이다.
- 2026-08-26 / M5-2 / PC 검증: 390×844에서 실제 두 회차를 연속 완료해 2개 기록 저장 직후 5분 제한, `오전 6:10에 다시 봐요`, 실시간 카운트다운, 비활성 CTA를 확인했다. 리필 카드 68px·CTA 54px이 154px 고정 푸터 안에서 겹치지 않았고 브라우저 경고·오류는 0건이었다.
- 2026-08-26 / M5-2 / 검증 한계: Analytics는 공식 정책상 라이브 환경에서만 전송·콘솔 확인이 가능하므로 로컬에서는 타입·payload와 실패 격리만 검증했다. 단계별 실제 복귀 데이터는 출시 후 확인한다.
- 2026-08-27 / 상태 점검 / 정리: 별도 원본 저장소에서 관리할 범용 하네스 작업 기록을 현재 제품 작업표·작업 로그에서 제거했다.
- 2026-08-27 / 상태 점검 / 검증: `. .\scripts\activate-dev.ps1; npm test`에서 0.3.0 테스트 35/35를 통과했다. 로컬 `sim-bwatta.ait`는 최신 소스보다 늦게 생성된 104,782바이트이며 SHA-256은 `B09AECE50EF9953E3DD669C5817AE2D2B32C91D075D04845D3ABDC5F405DB2E7`이다.
- 2026-08-27 / 공개 목표 재정렬 / 정정: 공식 챌린지 페이지를 다시 확인한 결과 1차 심사 지표 기간은 `9월 1일~9월 27일`이다. 9월 1일 이전 공개는 공식 마감 조건이 아니라 최대 지표 기간을 확보하기 위한 내부 목표로 정정했다.
- 2026-08-27 / M2-5 재판정 / 완료: M2-5는 0.2.1 번들의 핵심 루프·Storage·File·Share 실기기 검증이라는 원래 승인 기준을 충족해 REVIEWED를 유지한다. 0.3.0 리필 기능과 라이브 공개는 범위 밖이므로 M6~M8 후속 트랙으로 분리했다.
- 2026-08-27 / M6-1 / 공식 확인: 사업자 등록 없이도 현재 기능 범위의 공개가 가능하지만 게임 등급 증빙은 필수다. 등급은 동일 게임의 지원 스토어 실제 출시 URL 또는 게임물관리위원회 증명서로 제출한다.
- 2026-08-27 / M6-1 / 일정 판단: 게임물관리위원회 직접 심의는 공식 안내상 보통 10~15일, 앱인토스 출시 검수는 FAQ 기준 영업일 3~5일이다. 기존 Google Play 계정에서 동일 게임을 즉시 프로덕션 공개할 수 있는지가 최단 경로의 핵심 분기다.
- 2026-08-27 / M6-1 / 공개 체크리스트 감사: 현재 앱은 Safe Area와 핵심 기능 검증을 갖췄지만 핀치줌 방지와 운영체제 뒤로가기 제스처 비활성화가 없었다. 게임 내비게이션과 X 종료 모달은 SDK 3.x 설정과 실제 기기에서 별도 확인한다.
- 2026-08-27 / M6-2 / 공식·로컬 SDK 정정: 최신 웹 문서 일부는 구형 `webViewProps.type: game` 예시를 남기고 있지만 설치된 WebView SDK 3.1.1은 `webViewProps`를 `webView`로 바꾸고 `type`을 제거했다. 콘솔 앱 유형이 게임 내비게이션을 결정하며, SDK 2.9.2 이후 게임 X 버튼의 종료 확인 모달은 호스트가 제공하므로 앱에서 중복 구현하지 않는다.
- 2026-08-27 / M6-2 / 구현: `webView.allowsBackForwardNavigationGestures`를 `false`로 설정해 운영체제 뒤로가기·앞으로가기 제스처를 막고, viewport에 `maximum-scale=1`, `user-scalable=no`를 추가해 핀치줌을 막았다.
- 2026-08-27 / M6-2 / 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 37/37, TypeScript, Vite, `ait build`를 통과했다. 번들 내부 `webView.allowsBackForwardNavigationGestures: false`와 viewport 두 항목을 확인했다.
- 2026-08-27 / M6-2 / 산출물: 0.3.0 `sim-bwatta.ait`는 104,802바이트, deploymentId `01a04285-f037-726e-b011-cd30e091aed5`, SHA-256 `8BAF995B807000196BFAF5D958C1C5A38520E0F149DE3C1966C99C8D3CA6438C`다.
- 2026-08-27 / M6-3 / 작업 발행: 새 0.3.0 번들을 콘솔 테스트에 등록하고 게임 내비게이션·X 종료 확인 모달, 핵심 루프·Storage·File·Share, 두 회차 완료 후 5분 리필 제한을 실기기에서 회귀 확인한다.
- 2026-08-27 / M6-3 / 1차 실기기 QA: 사용자가 두 회차 완료 뒤 새 플레이 CTA가 차단되고 남은 시간 타이머가 정상 동작함을 확인했다. 대기 상태와 사용 가능 상태의 버튼이 시각적으로 비슷한 문제를 수정 대상으로 남겼다.
- 2026-08-27 / M6-3 / 리필 CTA 보정: 별도 리필 안내 카드를 제거하고 기존 하단 CTA 안에 `두루마리 휴지를 리필하고 있어요`, 절대 해제 시각, 초 단위 남은 시간을 두 줄로 통합했다. 사용 가능 CTA의 짙은 갈색과 구분되는 청회색 배경을 적용하고 비활성 상태의 투명도를 1로 고정했다.
- 2026-08-27 / M6-3 / 자동 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 37/37, TypeScript, Vite, `ait build`를 통과했다. 0.3.0 번들은 104,778바이트, deploymentId `01a042fb-daa1-7dfe-a74c-26fdc2793528`, SHA-256 `3012364091EC0497CC6A0CE90F529F8D2644C974998AEBF80F4FDB4420DDCF90`이다.
- 2026-08-27 / M6-3 / PC 시각 검증: 390×844에서 같은 제품 일자 두 회차를 완료해 5분 제한을 만들었다. 대기 CTA는 335×64px, 배경 `rgb(113, 132, 153)`, 흰색 문구, opacity 1, disabled 상태였고 안내·타이머가 버튼 안에서 겹침 없이 표시됐다.
- 2026-08-27 / M6-3 / 사용자 PC 승인: 사용자가 리필 안내를 기존 버튼 위치로 통합하고 사용 가능 상태와 색을 분리한 0.3.0 소스를 PC에서 확인해 의도대로 동작한다고 승인했다.
- 2026-08-27 / M6-3 / 버전 결정: 다음 공개 후보는 `0.3.1`로 확정했다. 리필 CTA 보정은 같은 0.3 기능 범위의 PATCH이며, 다른 공개 전 보완을 끝낸 뒤 한 번만 최종 빌드·업로드한다.
- 2026-08-27 / M6-3 / 공식 체크리스트 감사: 게임 출시 가이드는 사용자 식별자 조회·저장과 재접속 후 필요한 플레이 기록 유지를 요구한다. 현재 Storage 기록 유지는 구현됐지만 게임 사용자 식별키 호출은 없어 0.3.1 보완 대상으로 추가했다.
- 2026-08-27 / M6-3 / 로컬 설정 감사: `apps-in-toss.config.ts`의 `brand.primaryColor`가 템플릿 기본 파랑 `#3182F6`과 안내 주석으로 남아 있다. 앱의 실제 브랜드 기본색으로 교체하고 네이티브 게임 내비게이션의 로고·국문명과 함께 실기기에서 확인한다.
- 2026-08-27 / M6-3 / 배치 계획: 사용자 식별키를 핵심 플레이와 분리된 실패 안전 방식으로 로컬 보관하고, 브랜드 색상과 `package.json`·`package-lock.json` 0.3.1을 함께 반영한다. 이후 테스트·빌드·번들 내부 버전 검증을 한 번 수행한다.
- 2026-08-27 / M6 공식 일정 정정: 현재 공식 콘솔 등록 가이드는 앱 정보 검토를 영업일 1~2일, 출시 가이드는 번들 검토를 영업일 기준 최대 3일로 안내한다. 기존 FAQ 기준 3~5일 기록은 현재 일정 판단에 사용하지 않는다.
- 2026-08-27 / M6-3 / 구현: 앱 시작 시 SDK 3.1.1의 `User.getAnonymousKey()`로 게임 사용자 식별키를 조회해 `Storage`에 로컬 보관한다. 식별키는 UI와 Analytics에 넣지 않으며, PC·구버전·브리지 오류에서는 실패를 흡수해 핵심 플레이를 막지 않는다.
- 2026-08-27 / M6-3 / 설정: `brand.primaryColor`를 템플릿 파랑에서 앱의 갈색 `#332B24`로 바꾸고 `package.json`·`package-lock.json`·번들 내부 버전을 `0.3.1`로 맞췄다.
- 2026-08-27 / M6-3 / 자동 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 사용자 식별키 정상 저장·빈 값·브리지 실패를 포함한 테스트 40/40과 TypeScript·Vite·`ait build`를 통과했다.
- 2026-08-27 / M6-3 / 번들 검증: `sim-bwatta.ait` 내부 appName `sim-bwatta`, 앱 버전 `0.3.1`, SDK `3.1.1`, 브랜드 색상 `#332B24`, 뒤로가기 제스처 차단을 확인했다. 파일은 104,940바이트, deploymentId `01a04311-f7e7-7265-ac28-9a6fd8d153d7`, SHA-256 `A107F3750702B32B0BF6AE59F82A2874EC44ECBE7291D47C44B22ED86A13DED7`이다.
- 2026-08-27 / M6-3 / SDK 메타데이터 확인: AIT 메타데이터의 `isGame`은 CLI 3.1.1이 설정하지 않아 기본값 `false`로 생성된다. SDK 3.x는 구형 `webViewProps.type`을 제거했고 게임 여부는 콘솔 앱 유형에서 결정하므로 번들을 임의 수정하지 않는다.
- 2026-08-27 / M7 / 구성: 등급분류 경로 확정, 앱 정보, 번들 검토, 공개·라이브 확인을 사용자 작업 순서로 분리한 `docs/project/m7-release-checklist.md`를 추가했다. 현재 최우선 블로커는 동일 게임의 Google Play 공개 URL 또는 게임물관리위원회 등급분류증명서 확보다.
- 2026-08-27 / M7-1 / 경로 확정: 등급분류 증빙은 Google Play에 동일 게임을 실제 공개하는 경로로 진행한다. Google Play 앱 생성값은 앱 이름 `심봤다`, 패키지 이름 `com.double_b.sim_bwatta`, 기본 언어 한국어(`ko-KR`), 유형 게임, 가격 무료로 확정했다.
- 2026-08-27 / M7-1 / 상태 구분: 위 값은 사용자가 그대로 만들기로 확정한 입력값이다. Google Play Console 앱 생성 완료, 프로덕션 접근 권한, 실제 공개 URL과 자체등급분류 조회값은 아직 미확인으로 남긴다.
- 2026-08-27 / M7-1 / 계정 확인: 사용할 Google Play 개발자 계정이 개인 계정임을 확인했다. 공식 정책상 2023-11-13 이후 생성된 개인 계정은 최소 12명이 14일 연속 참여 상태를 유지하는 비공개 테스트 후 프로덕션 액세스를 별도로 신청해야 한다.
- 2026-08-27 / M7-1 / 절차 구분: 콘텐츠 등급 판정과 프로덕션 액세스는 별도다. 14일 요건 충족 뒤 신청 검토는 일반적으로 7일 이내지만 더 오래 걸리거나 테스터 참여가 부족하면 추가 테스트가 요구될 수 있다.
- 2026-08-27 / M7-1 / 일정 정정: 신규 개인 계정 요건이 적용되고 8월 27일에 12명이 모두 참여해도 신청 가능 시점은 빠르면 9월 10일 전후다. 따라서 9월 1일 이전 Google Play 공개 목표를 폐기하고, 비공개 테스트 즉시 시작과 승인 후 최단 공개를 우선한다.
- 2026-08-27 / M7-0 / 블로커 확정: 사용자가 해당 Google Play 개인 계정의 다른 앱에서도 12명·14일 비공개 테스트가 실제 프로덕션 블로커였음을 확인했다. 외주 테스트는 14일을 단축하지 못하고 조직 계정 전환은 D-U-N-S가 최대 30일 걸릴 수 있어 최단 경로에서 제외한다.
- 2026-08-27 / M7-0 / 대안 조사: Apps in Toss는 Microsoft Store 공개 URL을 등급 증빙으로 인정한다. Microsoft는 개인 개발자 무료 가입, PWA 게임 제출, IARC 설문과 최대 3영업일 인증을 공식 안내한다.
- 2026-08-27 / M7-0 / 추천: 현재 React 코드를 동일 게임 PWA로 분리해 Microsoft Store에 공개하는 경로를 1순위로 제안했다. Toss SDK 저장·파일·공유만 웹 대체 기능으로 분리하면 되므로 Android/iOS 포장보다 작다. 사용자 승인과 Partner Center 가입 가능 여부는 아직 확인 전이다.
- 2026-08-27 / M7-0 / 계정 원칙: 개발자 소유 계정과 공개 고객문의 연락처를 분리하고, 로그인·복구·신원 인증 정보와 계정 생성 장애 내역은 공개 문서에서 제외한다.
- 2026-08-27 / M7-0 / Microsoft 개발자 등록: 전용 개발자 계정으로 Partner Center 개인 등록·검증과 `Apps & Games` 진입을 완료했다.
- 2026-08-27 / M7-0 / 퍼블리셔 표기 확정: 공개 퍼블리셔명은 `double-B Std.`로 유지한다. 다른 스토어의 대소문자 정리는 출시 후 브랜드 정비로 분리하며 등급 증빙 블로커로 취급하지 않는다.
- 2026-08-27 / M7-0 / 경로 확정: 게임 등급 증빙을 현재 유일한 공개 블로커로 보고 Microsoft Store 동일 게임 PWA 경로를 최종 선택했다. Partner Center 개발자 등록 검증은 완료됐으며 다음 작업은 `심봤다` 이름 예약과 제품 식별자 확보다.
- 2026-08-28 / M6-3 / 실기기 회귀 진행: 0.3.1 핵심 기능은 현재까지 정상이며 실제 시간 대기가 필요한 항목을 계속 확인한다. 미니앱 내부 상단 X 버튼은 노출되지만 종료 확인 모달 없이 토스 네이티브 화면으로 즉시 전환돼 공식 게임 동작과 불일치한다.
- 2026-08-28 / M6-3 / 진단: 공식 릴리즈 노트는 게임 앱 X 버튼에 종료 확인 모달이 표시된다고 안내한다. 설치된 SDK·CLI 3.1.1은 구형 `type: game` 설정을 제거했고 현재 AIT 메타데이터 `isGame`은 기본 `false`이므로 번들을 임의 수정하지 않고 비공개 테스트에서 콘솔 게임 유형이 적용되는지 앱인토스에 문의한다.
- 2026-08-28 / M4 / 출품 자격 재확인: 공식 챌린지 안내는 8월 26일까지 신청폼 제출과 번들 등록을 요구하며 앱 정보 미승인 상태에서도 버전 등록 후 순차 검수를 기다리라고 명시한다. 검토 완료·출시는 8월 26일 자격 조건이 아니다.
- 2026-08-28 / M7-1 / Microsoft 제품 유형: 현재 Partner Center 드롭다운에서는 웹 기반 게임에 맨 아래 `MSIX 또는 PWA 게임`을 선택한다. `GDK game`은 새 PC 네이티브 게임 경로이고 `MSIX 또는 PWA 앱`은 비게임이므로 제외한다.
- 2026-08-28 / M6-3 / X 확인 이관: 비공개 테스트에서 게임 X 종료 확인 모달이 생략될 가능성을 공식 문서만으로 배제할 수 없다. 앱 코드가 아닌 토스 네이티브 셸 동작이고 현재 치명 오류가 아니므로 M6·M7 블로커에서 제외하고 M7-4 공개 라이브에서 재확인한다.
- 2026-08-28 / M7-1 / Microsoft 예약 이름 확정: Partner Center의 예약 `이름`은 패키지 식별자가 아니라 Store 고객에게 표시할 제품명이다. `심봤다`를 예약하며, Apps in Toss appName `sim-bwatta`와 Google Play 패키지명 `com.double_b.sim_bwatta`는 사용하지 않는다. Microsoft 제품 식별자는 이름 예약 뒤 자동 생성된 값을 사용한다.
- 2026-08-28 / M7-1 / Microsoft 제품 생성 완료: Partner Center 제품 개요에 `심봤다`, 유형 `게임`, 상태 `초안`, `제품 ID 보기`가 표시됐다. 이름 예약과 PWA 게임 제품 생성이 정상 완료된 것으로 판정하며 다음은 제품 식별값 확보다.
- 2026-08-28 / M7-1 / 식별정보 보관 결정: Microsoft 제품 식별자는 인증 비밀은 아니지만 개발자 계정·제품을 고정하므로 공개 문서나 추적 중인 `.memory`에 원문을 넣지 않는다. Git에서 제외된 `.local/microsoft-store/product-identity.md`에 전체 값을 보관하고, 공개 문서에는 확보 상태만 기록한다.
- 2026-08-28 / M7-1 / 작업 발행: 동일 React 핵심 게임을 유지하면서 Toss 전용 Storage·File·Share·Analytics·사용자 키를 웹 대체 동작으로 분기하고, PWA manifest·service worker·192/512 아이콘·`web-pwa` 빌드·GitHub Pages 배포 구성을 추가한다.
- 2026-08-28 / M7-1 / 구현: Vite `web-pwa` mode와 런타임 분기를 추가했다. 웹에서는 `localStorage`, PNG 다운로드, Web Share·클립보드, Analytics·사용자 키 no-op을 사용하고 Toss 빌드는 기존 SDK 동작을 유지한다.
- 2026-08-28 / M7-1 / PWA 구성: manifest, service worker, 192·512·maskable 아이콘과 GitHub Pages workflow를 추가했다. Pages 하위 경로 `/sim-bwatta/`를 기준으로 빌드하고 `dist-web/`은 Git에서 제외한다.
- 2026-08-28 / M7-1 / 자동 검증: 첫 `npm test`에서 Node 테스트가 확장자 없는 신규 runtime import를 해석하지 못한 4개 suite 오류가 발생했다. 신규 import를 명시적 `.ts`로 고친 뒤 `. .\scripts\activate-dev.ps1; npm test` 43/43, `npm run build:web`, `npm run build`를 모두 통과했다.
- 2026-08-28 / M7-1 / 번들 분리 검증: `dist-web`에서 Apps in Toss SDK·`intoss://`·게임 Analytics 이벤트 문자열이 발견되지 않았고 아이콘은 RGB 192×192, 512×512, maskable 512×512로 확인했다. 반대로 Toss `.ait`에는 manifest·service worker·PWA 아이콘이 포함되지 않았다.
- 2026-08-28 / M7-1 / PC 검증: `http://127.0.0.1:4173/sim-bwatta/`에서 manifest 연결, 메인 화면과 플레이 진입·롤 렌더링을 확인했고 브라우저 경고·오류는 0건이었다.
- 2026-08-28 / M7-1 / Toss 산출물 주의: 회귀 빌드가 로컬 `sim-bwatta.ait`를 105,194바이트, deploymentId `01a043e3-074a-70c3-a220-9d6c48269158`, SHA-256 `DBCCE1642C59391F2C1EB713061542C569CF3C7B6299683E06C850BE2A3B9C0F`로 새로 생성했지만 업로드하지 않았다. 콘솔에 업로드된 0.3.1 공개 후보는 deploymentId `01a04311-f7e7-7265-ac28-9a6fd8d153d7`, SHA-256 `A107F3750702B32B0BF6AE59F82A2874EC44ECBE7291D47C44B22ED86A13DED7` 그대로다.
- 2026-08-28 / M7-1 / 공개 저장소 정리: README를 게임 중심으로 개편하고 공개 가능한 스토어 자산을 `docs/assets/store`로 이동했다. 제품·기술·플랫폼 판단은 `docs/project`에 유지하되 개인 계정 운영 기록은 일반화하고 `.memory`는 Git 제외 로컬 인수인계 폴더로 전환한다.
- 2026-08-28 / M7-1 / 공개 전 검증: `. .\scripts\activate-dev.ps1; npm test`에서 43/43를 통과했다. 기존 Git 제외 `dist-web`의 로컬 권한 손상으로 기본 출력 경로 재생성은 실패했지만 `npm run build:web -- --outDir .local/verify-dist-web`은 TypeScript와 Vite 빌드를 통과해 소스·PWA 구성에는 이상이 없음을 확인했다.
- 2026-08-28 / M7-1 / 공개 정보 감사: `.memory`, `.local`, 생성물을 제외한 공개 대상 전체에서 이메일 주소, Microsoft 제품 식별자 원문, 사용자 홈 절대 경로와 일반 인증정보 패턴을 검색했고 0건을 확인했다.
- 2026-08-28 / M7-1 / 공개 이력 증류: 세션·하네스·행정 문서 대신 완료된 제품·UX·기술·QA 판단만 `DEVELOPMENT.md` 한 파일로 증류했다. 공개 스크린샷은 `assets/screenshots`로 분리하고 README는 표준 npm 실행법과 공개 개발 기록만 안내하도록 정리했다.
- 2026-08-28 / M7-1 / 문서 정책: `DEVELOPMENT.md`는 마일스톤 완료 시점에만 갱신한다. 계정·콘솔 식별자·신원 인증·출시 행정·로컬 환경·세션별 로그는 공개 저장소에 포함하지 않는다.
- 2026-08-28 / M7-1 / Pages 상태 분리: 단일 공개 커밋 `ecf6706`의 원격 `main` 푸시는 성공했지만, 그 푸시가 실행한 `Deploy web PWA`는 build job이 17초에 실패해 deploy job이 건너뛰어졌다. 따라서 저장소 정리·푸시는 완료, GitHub Pages 공개는 미완료로 구분한다.
- 2026-08-28 / M7-1 / 다음 확인: 저장소를 Public으로 전환하고 `Settings → Pages → Build and deployment → Source: GitHub Actions`를 설정한 뒤 실패 실행을 재시도한다. 이미 설정했다면 build job의 세 annotation 원문을 확인해 워크플로 원인을 별도로 진단한다.
- 2026-08-28 / M7-1 / Pages 공개 완료: Source를 GitHub Actions로 전환한 뒤 실패 실행 `33102030328`을 전체 재실행했다. build 17초, deploy 9초로 모두 성공했고 `https://le-corl.github.io/sim-bwatta/`에서 메인·플레이 진입, manifest, `sw.js` 응답을 확인했다.
- 2026-08-28 / M7-1 / PWABuilder 진단: 공개 URL과 manifest는 패키징 가능 판정을 받았지만 service worker는 미감지됐다. 현재 등록이 `window.load` 뒤 실행되는 구조이고 PWABuilder는 즉시 등록을 권장하므로, 웹 빌드에만 `<head>` 선등록을 주입하고 React 진입부의 지연 등록을 제거했다.
- 2026-08-28 / M7-1 / 자동 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build:web -- --outDir .local/verify-pages-sw`에서 테스트 43/43과 TypeScript·Vite 웹 빌드를 통과했다. 생성된 `index.html`의 첫 스크립트가 `/sim-bwatta/sw.js`를 scope `/sim-bwatta/`로 즉시 등록하는지 확인했다.
- 2026-08-28 / M7-1 / 배포 검증: service worker 즉시 등록 변경을 GitHub Pages에 반영한 뒤 PWABuilder 재검사에서 필수 Action Item 0건, manifest·service worker·offline support 통과를 확인했다.
- 2026-08-28 / M7-1 / 패키징 우회: PWABuilder 웹 화면의 `Package For Stores` 버튼이 대화상자를 열지 않고 직접 패키징용 `/portals` 경로도 `FindWebManifest`에 405를 반환했다. 앱 판정 문제가 아니므로 공식 프런트엔드가 사용하는 Microsoft PWABuilder Windows API에 동일 옵션을 전송했다.
- 2026-08-28 / M7-1 / Store 패키지 생성: Windows Store 패키지 버전 `1.0.1`과 classic 버전 `1.0.0`으로 패키지를 생성했다. 외부 ZIP은 14,678,612바이트, SHA-256 `CBFAA8A5D4787BDABB939B87D7B043E7E1C3CFA321482732B85DE0CDBC2F4FAD`이며 Git 제외 `.local/microsoft-store`에 보관한다.
- 2026-08-28 / M7-1 / 패키지 검증: Store 업로드 대상 `심봤다_1.0.1.0.msixbundle`은 4,879,555바이트, SHA-256 `CCA08BE578984FDE6CFBC77D3B4C9EAEFC1344720DCB0BA12AE057041A987484`다. 번들 manifest에서 예약 Package ID·Publisher ID·`double-B Std.`·제품명 `심봤다`·버전 `1.0.1.0`·`KO-KR`·GitHub Pages 시작 URL이 모두 일치함을 확인했다.
- 2026-08-28 / M7-1 / 개인정보 처리방침 결정: Microsoft Store의 텍스트 입력에만 종속하지 않고 Apps in Toss에서도 재사용할 수 있는 단일 공개 URL을 기준으로 삼는다. 한 문서 안에서 공통 로컬 기록, Apps in Toss 익명 식별키·Analytics, Web PWA의 GitHub Pages 접속 정보 처리를 구분한다.
- 2026-08-28 / M7-1 / 개인정보 처리방침 구현: `public/privacy.html`에 게임명·운영자·공개 문의처, 처리 항목·목적·저장 위치·삭제 방법, 외부 플랫폼, 이미지 저장·공유를 명시했다. 보조 HTML 경로가 메인 경로 캐시를 덮어쓰지 않도록 service worker의 탐색 응답을 요청 URL별로 캐시하고 캐시 버전을 올렸다.
- 2026-08-28 / M7-1 / 개인정보 처리방침 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build:web`에서 테스트 43/43과 웹 빌드를 통과했다. 로컬 HTTPS 대상과 동일한 `/sim-bwatta/privacy.html` 경로를 데스크톱·390×844에서 검수해 제목·본문·외부 링크·문의처, 페이지 가로 넘침 없음과 콘솔 경고·오류 0건을 확인했다.
- 2026-08-28 / M7-1 / 개인정보 용어 교정: 배포 변형을 직역한 기존 접미 표현을 제거하고 `Apps in Toss 플랫폼`, `Microsoft Store/Web PWA 플랫폼`으로 바꿨다. 개인정보 처리방침과 M7 체크리스트에서 동일한 용어를 사용한다.
- 2026-08-28 / M7-1 / 향후 갱신 명세: `brief.md`에 개인정보 처리방침을 출시 게이트로 추가했다. `0.4.x` 수집, `0.5.x` 리워드, `0.6.x` 사용자 메모, `0.7.x` 로그인·푸시·서버의 예상 데이터 변화와 구현 전 확인 항목을 구분했으며, 미구현 기능은 현재 공개 방침에 미리 적지 않는 원칙을 확정했다.
- 2026-08-28 / M7-1 / 용어·문서 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build:web`에서 테스트 43/43과 TypeScript·Vite 웹 빌드를 통과했다. 생성된 `dist-web/privacy.html`에서 두 플랫폼 명칭과 공개 문의처를 확인했다.
- 2026-08-28 / M7-1 / Microsoft 제출 완료: PWABuilder `.msixbundle`, IARC 연령 등급, `인디 + 시뮬레이션`, 한국어 설명·기존 플레이 스크린샷 3장, 개인정보 처리방침, 대한민국 한정·공개 검색·무료·즉시 출시 설정을 입력해 제품 릴리스를 인증 제출했다. Xbox 장치·서비스·GDK를 사용하지 않아 Xbox 크리에이터스 프로그램은 시작하지 않았다.
- 2026-08-28 / M7-1 / 외부 대기 전환: Partner Center 제품 상태는 `인증 중`, 인증 단계는 제출 완료 뒤 `사전 처리 중`이다. Microsoft 인증과 자동 게시가 끝나기 전에는 실제 Store 공개 URL과 등급 증빙을 확보한 것으로 판정하지 않으며 M7-1은 DOING을 유지한다.
- 2026-08-28 / M6-3 / 공유 카드 QA: Apps in Toss 실기기에서 PNG 저장과 결과 문구·링크 공유는 정상 동작했다. 저장된 1080×1350 카드의 휴지심만 현재 결과 UI와 달리 M2-UI-2 이전의 가로 원통을 사용해 시각 회귀로 판정했다.
- 2026-08-28 / M6-3 / 원인·수정: `resultCard.ts`가 DOM의 `.result-core`를 재사용하지 않고 예전 가로 원통을 Canvas 도형으로 별도 렌더링하고 있었다. 카드 도형을 현재 UI와 같은 세로 원통, 상단 타원, 굽은 이음선, 일반·황금 팔레트로 교체하고 저장·공유 호출은 변경하지 않았다.
- 2026-08-28 / M6-3 / PC 검증: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 43/43과 TypeScript·Vite·AIT 빌드를 통과했다. 브라우저에서 기존 기록으로 새 PNG를 생성해 세로 비율·상단 타원·이음선·3줄 문구·레이아웃을 육안 확인했다. 이미 업로드한 0.3.1 이후 수정이므로 공개 후보를 0.3.2로 올리고 실기기 이미지 재확인을 남겼다.
- 2026-08-28 / M6-3 / 0.3.2 번들: 패키지·lockfile·`.ait` 내부 버전을 0.3.2로 맞추고 테스트 43/43과 빌드를 다시 통과했다. `sim-bwatta.ait`는 105,467바이트, deploymentId `01a046b2-644b-7f25-bdd5-3311318aba5a`, SHA-256 `9B2B2426FCAD9BF04330402C1CC5435C47C7A296569C6202DDC54C49E1F48433`이며 콘솔 업로드 전이다.
- 2026-08-28 / M6-3 / 0.3.2 시각 QA: 세로 원통 전환은 반영됐지만 저장 PNG의 휴지심이 앱 상세 화면보다 짧고 넓어 보였다. `수직 원통` 자체가 아니라 기존 `.result-core`의 가늘고 긴 휴지심 실루엣을 기준으로 다시 맞추기로 했다.
- 2026-08-28 / M6-3 / 0.3.3 비율 보정: Canvas 휴지심을 너비 400px, 본체 높이 520px, 상단 타원 반경 50px로 조정해 외곽 기준 약 1.43:1 세로 비율을 만들었다. 이음선과 문구 중심을 함께 내리고, 하단 결과·미션 영역을 재배치해 앱 상세 화면의 기존 휴지심 비율을 유지했다.
- 2026-08-28 / M6-3 / 0.3.3 PC 검증: 기존 보관함 기록에서 실제 `이미지 저장` 동작으로 1080×1350 PNG를 다시 생성했다. 앱 상세 화면과 같은 가늘고 긴 실루엣을 확인하고, 길어진 휴지심 그림자와 결과 제목이 닿던 부분은 하단 정보 영역을 내려 겹침 없이 보정했다.
- 2026-08-28 / M6-3 / 0.3.3 번들: `. .\scripts\activate-dev.ps1; npm test; npm run build`에서 테스트 43/43과 TypeScript·Vite·AIT 빌드를 통과했다. 패키지·lockfile·`.ait` 내부 버전은 0.3.3이며 `sim-bwatta.ait`는 105,462바이트, deploymentId `01a046c2-053f-7cc4-bc76-87b91dfe08bc`, SHA-256 `AE78DB842004F1AC48E39C4A056CF3C36A8F96089068214268C6784D9B7871CF`이다. M6-3은 새 번들 업로드와 실기기 PNG 비율 확인까지 BLOCKED를 유지한다.
- 2026-08-28 / M6-3 / 0.3.3 하단 곡률 QA: 세로 비율 보정 결과의 하단 윤곽이 수평에 가깝게 보여 휴지심보다 잘린 상자처럼 읽혔다. 크기와 비율은 유지하고, 타원의 4분원 베지어 근사값을 사용해 양옆보다 중앙이 52px 내려오는 둥근 밑면으로 보정했다.
- 2026-08-28 / M6-3 / 0.3.3 하단 곡률 검증·재빌드: 기존 기록으로 실제 저장 PNG를 생성해 이미지 2와 같은 아래로 볼록한 밑면과 제목 간격을 확인했다. 테스트 43/43과 빌드를 다시 통과했으며 최종 `sim-bwatta.ait`는 105,490바이트, deploymentId `01a046c9-54db-7868-8526-537b1df204fe`, SHA-256 `C78BA3726003A37AC06EDA9A810288EFAE121AC2102B64C80C352EF53E7A8F71`이다.
- 2026-08-28 / M6-3 / 실기기 승인·M6 종료: 사용자가 0.3.3을 Apps in Toss 콘솔에 업로드하고 실기기에서 최종 공유 PNG를 확인해 이상 없음을 승인했다. 저장·공유·리필 회귀와 공유 카드 시각 보정이 모두 통과했으므로 M6-3을 REVIEWED, M6을 DONE으로 종료한다. 다음 단일 진행 작업은 Microsoft 인증·게시와 등급 증빙을 기다리는 M7-1이다.
- 2026-08-28 / M6-3 / 플랫폼 반영 범위: 공유 카드는 Toss와 Microsoft Store/Web PWA가 공통 `resultCard.ts`를 사용하므로 0.3.3 보정이 양쪽 빌드에 동일하게 포함된다. Toss는 업로드·실기기 확인을 완료했고, Web PWA는 `main` 푸시와 GitHub Pages 배포가 끝난 시점에 실제 사용자 환경에 반영된다.
- 2026-08-28 / M6-3 / Web PWA 빌드 검증: 기존 `dist-web/icons` 출력 폴더가 Windows에서 잠겨 표준 출력 경로 정리는 실패했지만, 생성물을 삭제하지 않고 `. .\scripts\activate-dev.ps1; npm run build:web -- --outDir .local/qa-dist-web-0.3.3`으로 동일 웹 빌드를 별도 경로에서 통과했다. 0.3.3 공유 카드 코드는 Web PWA 번들에도 포함된다.
